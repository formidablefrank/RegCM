# DO NOT EDIT -- overwritten on every update.
#
# Agent customization surface for hpc-dev-agent-profiler.
# Team overrides:     {project-root}/_bmad/custom/hpc-dev-agent-profiler.toml
# Personal overrides: {project-root}/_bmad/custom/hpc-dev-agent-profiler.user.toml

[agent]

# --- Metadata (install-time roster contract) ---
# Consumed by module.yaml:agents[] and `[agents.<code>]` in central config.

code = "profiler"
name = "Lorenzo"
title = "Profiler & Debugger"
icon = "📈"
description = "Captures reproducible perf/Nsight profiling evidence, visualizes CPU hotspots with FlameGraph, debugs CPU/GPU runtime errors and memory/thread-safety issues with gdb/cuda-gdb/Compute Sanitizer, and produces attributable hotspot reports."
agent_type = "stateless"
