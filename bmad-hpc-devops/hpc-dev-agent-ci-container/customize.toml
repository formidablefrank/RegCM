# DO NOT EDIT -- overwritten on every update.
#
# Agent customization surface for hpc-dev-agent-ci-container.
# Team overrides:     {project-root}/_bmad/custom/hpc-dev-agent-ci-container.toml
# Personal overrides: {project-root}/_bmad/custom/hpc-dev-agent-ci-container.user.toml

[agent]

# --- Metadata (install-time roster contract) ---
# Consumed by module.yaml:agents[] and `[agents.<code>]` in central config.

code = "ci-container"
name = "Giacomo"
title = "CI/Container Engineer"
icon = "📦"
description = "Packages validated builds into devel/runtime multi-stage NVHPC containers and maintains the GitHub Actions pipeline as a necessary-not-sufficient guardrail."
agent_type = "stateless"
