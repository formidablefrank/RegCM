---
scope: RegCM5 HPC Modernization Architecture
purpose: Architecture spine fixing the invariants for the HPC modernization program scoped in prd-RegCM-2026-07-04 (9 features, FR-1-38, 4 phases): CPU baseline strategy, MPI/threading strategy, I/O scaling, compiler/GPU candidate selection, test strategy, numerical reproducibility policy, profiling method, build matrix, phased migration plan.
altitude: initiative (whole modernization program, above all 4 PRD phases)
updated: 2026-07-05T20:15
---

- (constraint) Working mode: Coaching path. Deliverable: spine + fuller architecture doc (both confirmed by regcm5-dev).
- (constraint) Driving input: _bmad-output/planning-artifacts/prds/prd-RegCM-2026-07-04/prd.md (status: final, 9 features, FR-1-38, FR-36 retired, 4 phases) plus its .memlog.md. Standing brownfield fact base: _bmad-output/project-context.md (112 rules).
- (decision) [ADOPTED] Paradigm: Fortran HPC batch SPMD model, 2D MPI domain decomposition (iy,jx) via mod_dynparam, do-concurrent-unified CPU/GPU loop-parallelism (NVHPC compiles the same source to -stdpar=multicore or -stdpar=gpu). Read off the existing code and project-context.md, not re-derived.
- (version) Target stack (from project-context.md / PRD Technology and Dependency Targets): NVHPC 26.3, HPC-X 2.25, CUDA 12.9, binutils 2.42, HDF5 1.14.3, netCDF-C 4.9.2, netCDF-Fortran 4.6.1, PnetCDF 1.12.3. GNU and Intel required minimum; AMD/AOCC 'where feasible' (non-mandatory, PRD Non-Goals).
- (decision) [ADOPTED] Numerical reproducibility is two-tier (PRD FR-25/FR-37): CPU-only changes bit-exact by default; CPU-to-GPU porting specifically accepted at thesis-measured statistical bounds (tas/ps rMAD/rRMSE <0.1%, huss <5%, over a 7-model-day run), enforced going forward by FR-37's regression test. Not open for re-litigation here.
- (decision) [ADOPTED] Profiling method (PRD FR-1/FR-4, thesis-sourced): perf with --call-graph dwarf (requires -g) + Brendan Gregg FlameGraph rendering for CPU stacks; NVIDIA Nsight Systems for GPU/compiler-runtime symbol attribution perf cannot resolve; -Minfo=accel to confirm what a region actually offloaded.
- (decision) [ADOPTED] Accelerator strategy: do concurrent is the sole loop-parallelism/threading construct; OpenACC is an escalation path used only after profiling shows do concurrent insufficient (already the merged pattern for the 4 confirmed hotspots: getcape, CLM-hydrology memset, interp1d_r8, heatindex). No OpenMP, no OpenMP-target, no CUDA Fortran.
- (decision) [ADOPTED] CI guardrail (PRD Constraints): a green GitHub Actions check (FR-31/32, GNU+Intel container builds + regression-diff) is necessary but not sufficient acceptance evidence. Final acceptance for physics/dynamics/accelerator changes requires validation on dcgp_usr_prod (Intel, ICT26_MHPC_0) and boost_usr_prod (NVHPC, ICT26_MHPC) via Slurm.
- (decision) [ADOPTED] Phased migration plan follows PRD Phased Scope verbatim: Phase 1 Foundation (profiling FR-1-4, regression infra FR-24-27, namelist/compiler-id/doc fixes), Phase 2 I/O and Memory Hardening (FR-5-7, FR-33-34, FR-19), Phase 3 Selective Acceleration and Containerization (FR-35, FR-37, FR-8-12, FR-22, FR-28-32), Phase 4 External Coupling (FR-13-17), sequenced last by priority choice not technical necessity.
- (question) GPU-port candidacy formula (go/no-go criteria beyond FR-9's one-off RRTMG investigation): regcm5-dev deferred -- I/O scaling and performance optimization are the priority right now, not formalizing GPU-porting selection criteria. Not an AD in this spine; listed under Deferred, revisit when Phase 3 GPU work resumes.
- (direction) regcm5-dev's priority ordering for this spine and the near-term work it governs: I/O scaling and performance optimization first, ahead of GPU-porting-selection formalization. Coaching depth should follow that weighting.
- (decision) I/O opt-in mechanism: each output path (diagnostic/history FR-5-6, restart/checkpoint, CLM land-model history FR-33) gets its own independent namelist switch for parallel vs serial write, not one unified switch -- since the three paths resolve on different timelines (diagnostic ships now as tested-non-default; restart may already partially use PnetCDF; land-model output depends on CLM coupling being active) and FR-34's cause investigation may clear them at different times.
- (decision) Build matrix: every native NVHPC build tests both --enable-openacc-managed and --enable-openacc-stdpar variants as explicit, required matrix cells -- not one sanctioned default with the other as an occasional check. Both ship as tested configurations.
- (question) Halo-exchange GPU-direct-drop-in rule (mod_mppparam exchange_array_* signatures frozen against a future GPU-direct RDMA swap): read regcm5-dev's echoed-question-back as agreement that fixing this now is overengineering, since FR-11 is investigation-only with no implementation committed in this program. NOT written as an AD -- listed under Deferred, revisit if/when FR-11's investigation recommends implementation. Flagged explicitly for regcm5-dev to correct if the read is wrong.
- (decision) CPU baseline strategy (canonical reference for all I/O and performance claims): Testing/CORDEX/EUR-03_RegCM5_ERA5.namelist fixture (1003x1003x50, ROTLLR, ERA5-driven CORDEX EUR-03 domain -- the same 1003x1003x50 grid the thesis profiled), run at 4 nodes on dcgp_usr_prod (Intel). Input dataset: http://clima-dods.ictp.it/Users/ggiulian/transfer/EURR12/, only files the fixture needs are downloaded, to /leonardo_scratch/fast/ICT26_MHPC_0/regcm5-dev/RegCM-data/input. Any I/O-scaling or performance claim not measured against this fixture/node-count/partition is not comparable evidence.
- (decision) Regression baseline storage (FR-27): trusted baseline outputs live at /leonardo_scratch/fast/ICT26_MHPC_0/regcm5-dev/RegCM-data/output (Leonardo scratch_fast, not in-repo/git-LFS). Enforcement mechanism built and self-tested: Tools/Scripts/TestingAndBenchmarking/manage_baseline.py promote/log -- refuses to overwrite an existing baseline file without --force, refuses --force without --reason, and appends every promotion (timestamp, user, RegCM git commit, reason, files touched) to BASELINE_MANIFEST.jsonl alongside the baseline. This is the explicit-sign-off record FR-27 requires, in place of a silent overwrite.
- (constraint) Baseline storage sits on Leonardo scratch_fast, which is typically subject to a cluster retention/purge policy distinct from project/work space -- not yet verified against $HPCDOCS. Flagged as a Deferred item, not assumed safe long-term.
- (decision) I/O opt-in mechanism confirmed against live code: RegCM already follows a per-path independent-switch pattern -- do_parallel_netcdf_in (ICBC/boundary input, mod_params.F90/mod_clm_params.F90) and do_parallel_netcdf_out feeding do_parallel_save (restart/savefile, mod_savefile.F90) are already two separate namelist switches, not one. mod_ncout.F90 (diagnostic/history output, FR-5) currently has no switch at all -- FR-5 must add a new one, following the existing do_parallel_netcdf_<path> naming convention, not invent a different shape. This directly validates the independent-switch-per-path decision already logged.
- (event) manage_baseline.py re-tested end-to-end under 'module load python/3.11.7' (the project's documented Python runtime for cluster script execution) -- identical behavior to the default login-shell python3 3.6.8: refuse-without-force, refuse-force-without-reason, promote+manifest, log replay, and missing-run-dir all pass. No code changes needed; stdlib-only, no syntax past 3.6.
- (constraint) Corrected via $HPCDOCS/hpc/hpc_data_storage.html (prior flag was wrong): /leonardo_scratch/fast/ICT26_MHPC_0/regcm5-dev/RegCM-data/ sits under $FAST, which is PERMANENT/shared-per-project, NOT the 40-day-purge $SCRATCH area (a same-named but distinct area at /leonardo_scratch/large/userexternal/jrayo000). $FAST retention = life of project ICT26_MHPC_0 + 6 months after project end. Real constraints instead: (1) no backup at all in $FAST -- an accidental delete or bad --force overwrite via manage_baseline.py is unrecoverable, the manifest only tells you what to recompute; (2) fixed 1 TB quota for the WHOLE project ICT26_MHPC_0, shared with everything else on $FAST, extensions not available per CINECA policy -- baseline retention/rotation (how many generations of a 1003x1003x50 CORDEX fixture's diagnostic+restart output to keep at once) needs a policy, not unbounded accumulation; (3) retention ends when project ICT26_MHPC_0 ends (+6mo grace) -- someone needs to track that date so baselines aren't silently cancelled.
- (decision) SUPERSEDES prior CPU-baseline-strategy entry. Canonical baseline fixture: Testing/EUR12_namelist.in (confirmed present, 275x275x36, ROTLLR, EURR-12 domain -- smaller/faster than the earlier-considered CORDEX EUR-03 1003x1003x50 fixture), run at 2 nodes on dcgp_usr_prod (Intel). Any I/O-scaling or performance claim not measured against this fixture/node-count/partition is not comparable evidence.
- (decision) Baseline retention/rotation policy: superseded baseline generations are archived, not discarded, to /leonardo_work/ICT26_MHPC_0/regcm5-dev/RegCM-archive/baseline -- $WORK (permanent per-project, retained until 6 months after project ICT26_MHPC_0 ends), chosen over the initially-proposed /leonardo_scratch/large/userexternal/jrayo000 path after $HPCDOCS confirmed that path is $SCRATCH proper (40-day auto-purge on inactivity, no backup), which would have silently deleted 'archived' baselines -- defeating the purpose. manage_baseline.py extended and tested: promote --force now additionally requires --archive-dir (copies superseded files to archive/<UTC-timestamp>-<commit>/ before overwrite) or an explicit --no-archive opt-out; refusal, archival, and opt-out paths all verified under python/3.11.7.
- (decision) Build-matrix partition assignment: GNU-compiled builds validate on dcgp_usr_prod alongside Intel (both CPU-path vendors share that partition/QoS/account per project-context.md's existing Intel rule). NVHPC-compiled builds remain on boost_usr_prod. AMD/AOCC: no assigned partition -- non-mandatory per PRD Non-Goals, revisit only if an AMD allocation becomes available (PRD Open Question 4).
- (decision) Version-pinning policy is asymmetric by design, confirmed with regcm5-dev: NVHPC (26.3)/HPC-X (2.25)/CUDA (12.9) and all I/O libraries (HDF5 1.14.3, netCDF-C 4.9.2, netCDF-Fortran 4.6.1, PnetCDF 1.12.3) are hard-pinned identically across native builds AND all three container variants -- a version bump requires the same regression evidence as any other stack change (build+test+numerical+performance record). GNU and Intel compiler versions specifically are NOT hard-pinned -- containers use the latest compiler release verified compatible with the pinned I/O library versions, re-verified at each container rebuild rather than fixed once.
- (version) Web-verified (this session): GCC/gfortran 16.1 (released 2026-04-30) is current for GNU containers -- no known incompatibility with HDF5 1.14.3/netCDF-C 4.9.2/netCDF-Fortran 4.6.1/PnetCDF 1.12.3. Intel oneAPI Toolkit 2026.0 / ifx 2026.0.0 is current for Intel containers -- fully supports Fortran through 2018 plus partial 2023, comfortably covers RegCM5's F90/2003/2008 requirement. Caveat: ifx needs explicit FC=ifx F77=ifx CC=icx and --disable-fortran-type-check to build netCDF-Fortran 4.6.1/PnetCDF 1.12.3 cleanly (known sizeof(off_t) configure issue; PnetCDF's configure silently defaults to gfortran if FC isn't set explicitly) -- this is a build-matrix note, not just a version pin. Sources: intel.com Fortran Compiler 2026 release notes, gcc.gnu.org/releases.html, Intel Community forum (pnetcdf configure error using icx and ifx), Unidata netcdf-fortran release notes/issue #436.
- (decision) SUPERSEDES prior nproc=2 entry. Multi-process-count regression standard (FR-26, project's stencil/halo rule): fixed minimum pair is nproc=1 and nproc=4 -- confirmed via code read of Main/mpplib/mod_mppparam.F90:1366-1399 (set_nproc) that RegCM5's auto-decomposition only performs a genuine 2D split (both jx and iy directions) at nproc>=4; for nproc<4 (including nproc=2/3) cpus_per_dim(2)=1, so the iy-direction halo exchange is never exercised and a decomposition bug there would be as invisible as at nproc=1. nproc=4 is the smallest process count where both decomposition directions are auto-exercised, so it is the fixed second point everyone uses for comparable baselines -- not nproc=2.
- (decision) GPU-port candidacy: confirmed case-by-case, not a fixed formula -- each subsystem gets its own FR-9-style investigation (go/no-go checkpoint) rather than a reusable scoring rule. This is a deliberate, standing policy (not merely deferred pending later formalization); revisit only if case-by-case review becomes a bottleneck.
- (decision) CI regression scope is a separate track from the HPC canonical baseline: CI (GitHub Actions, FR-31) uses Testing/ideal.in (confirmed present: 100x500x60 idealized MOLOCH domain, NORMER projection, no ICBC/lateral forcing -- cheap enough for GitHub-hosted runners) as its own fixture with its own trusted baseline, stored in-repo (not in $FAST/manage_baseline.py's external HPC-scoped storage). The EUR12_namelist.in/2-node/dcgp_usr_prod baseline remains the canonical reference for I/O-scaling and performance claims specifically; ideal.in's in-repo baseline is for CI correctness-gating on every push/PR, a distinct concern from performance evidence.
- (decision) CI baseline (Testing/ideal.in) storage: plain git, no git-lfs -- accepted tradeoff since ideal.in's output is small (idealized 100x500x60 domain, no ICBC) and baseline updates should be rare (only on reviewed, intentional changes), not worth adding a git-lfs tooling dependency (not currently installed in this environment) for that volume. Revisit if baseline update frequency or file size grows materially.
- (decision) Refines (not supersedes) the EUR12_namelist.in canonical-baseline entry: confirmed two distinct copies by design -- Testing/EUR12_namelist.in (in-repo, version-controlled, relative dirter/dirglob/dirout paths, defines the fixture's physics/domain parameters as the durable reference) versus /leonardo_scratch/fast/ICT26_MHPC_0/regcm5-dev/RegCM-data/EURR12/EUR12_namelist.in (the actual runnable deployment copy, same 275x275x36/EURR-12/ROTLLR domain, with dirter/inpter/dirglob/inpglob resolved to real $FAST-area absolute paths and input data already partially staged under RCMDATA/). The repo copy is the canonical parameter reference; the RegCM-data copy is what actually executes for baseline generation.
- (decision) Profiling-artifact storage mirrors baseline storage exactly, confirmed by regcm5-dev: perf.data, FlameGraph SVGs, Nsight .nsys-rep files, and written hotspot reports (FR-1-4) live at /leonardo_scratch/fast/ICT26_MHPC_0/regcm5-dev/RegCM-data/profiling ($FAST), archived on supersession to /leonardo_work/ICT26_MHPC_0/regcm5-dev/RegCM-archive/profiling ($WORK) -- same rationale as baseline archival ($FAST has no backup and a fixed 1 TB project quota; $WORK survives project end +6mo). Reuses manage_baseline.py as-is (already fully generic: promote/--archive-dir/--no-archive/log take arbitrary --run-dir/--baseline-dir pairs). Renamed the manifest constant from BASELINE_MANIFEST.jsonl to PROMOTION_MANIFEST.jsonl to reflect this dual use accurately; re-tested after rename, unaffected.
- (decision) First-cut regression-diff tool built (FR-24/FR-25): Tools/Scripts/TestingAndBenchmarking/regression_diff.py, PEP-723 inline script metadata (dependencies = netCDF4, numpy) so 'uv run regression_diff.py ...' resolves its own deps ephemerally -- no HPC module or conda env needed (system python3 has neither netCDF4 nor numpy installed; uv fetched both in <1s during testing). Compares every .nc file in a candidate run dir against a baseline dir field-by-field: reports MAD/RMSE/rMAD/rRMSE per variable (never a single pass/fail bit), applies the two-tier policy (bit-exact default; tas/ps <0.1%, huss <5% GPU-port statistical tolerance from FR-25/FR-37, overridable via --tolerance-file), flags shape mismatches and missing variables/files distinctly, supports --only-fields and --json. This is the mechanical enforcement FR-25's tolerance policy previously only had as prose.
- (event) regression_diff.py self-tested against 8 synthetic netCDF4 scenarios (all passed as expected): bit-exact match; a bit-exact-tier field differing by 1e-9 correctly FAILs (proves the default really is zero-tolerance, not 'close enough'); a GPU-tier field (tas) at 0.03% divergence PASSes against the 0.1% bound; the same field at 1% divergence FAILs; a missing variable and a missing whole file both FAIL with distinct status; --only-fields correctly restricts scope; --tolerance-file correctly overrides the default table; --json produces machine-readable output. Not yet run against real RegCM5 output (no baseline generated yet) -- validated on synthetic fixtures only.
- (decision) CLARIFIES prior refinement entry -- removes ambiguity. THE canonical baseline (single source of truth for all I/O-scaling and performance claims) is /leonardo_scratch/fast/ICT26_MHPC_0/regcm5-dev/RegCM-data/EURR12/EUR12_namelist.in (2 nodes, dcgp_usr_prod) -- confirmed explicitly by regcm5-dev, not 'the repo copy is canonical.' Testing/EUR12_namelist.in is a version-controlled reference copy of the same domain/physics parameters (275x275x36, EURR-12, ROTLLR) for documentation/portability, not itself a competing canonical designation. CI uses a separate, smaller fixture: Testing/ideal.in (confirmed unchanged from earlier decision), with its own in-repo baseline, plain git.
- (decision) Fixed the per-nproc directory convention (FR-26), confirmed by regcm5-dev: any run/baseline/archive/profiling collection that spans multiple process counts uses nproc-<N>/ subdirectories (e.g. <dir>/nproc-1/, <dir>/nproc-4/) consistently across manage_baseline.py and regression_diff.py. manage_baseline.py's promote/log gained --nproc N (operates on the nproc-N/ subdir of --run-dir/--baseline-dir/--archive-dir); tested: promote nproc-1 and nproc-4 independently, log --nproc 1 shows only that history, overwriting nproc-1 with --archive-dir correctly scopes the archive under archive/nproc-1/ and leaves nproc-4's baseline untouched.
- (decision) Confirmed by regcm5-dev: nproc=1-vs-nproc=4 comparison uses the SAME two-tier tolerance table as any other comparison (bit-exact default, GPU-port statistical bounds for tas/ps/huss) -- no third/separate tier for decomposition-count divergence. Rationale (confirmed, not just assumed): RegCM5's decomposition is local-stencil-based, halo exchange exists precisely so a grid point sees correct neighbor values regardless of rank count, so a bit-exact-tier field diverging between process counts indicates an actual decomposition bug, not an accepted artifact.
- (decision) regression_diff.py extended with --nprocs (comma-separated counts, e.g. 1,4) for FR-26's single-invocation multi-process-count comparison -- reads nproc-<N>/ subdirs of --run-dir/--baseline-dir per the new convention, aggregates pass/fail across all counts, nests per-count results in both text and JSON output. Also added --report-file to persist the JSON report to disk for citation in a commit/PR's acceptance record (Build/Test/Numerical/Performance), independent of --json (which controls stdout format only).
- (event) Both --nprocs and --report-file tested end-to-end with synthetic fixtures: nproc-1 comparison passed (bit-exact), nproc-4 comparison correctly FAILed on an injected tas divergence (0.17% against tas's 0.1% GPU-tier bound), demonstrating the tool would actually catch a real decomposition bug at the process count where 2D decomposition is exercised. --report-file correctly persisted the full nested JSON to disk.
- (decision) Fixed exit-code convention (confirmed by regcm5-dev), applied to both scripts: 0 = pass, 1 = regression/policy failure, 2 = usage/configuration error. manage_baseline.py never itself judges a regression, so every one of its error paths (missing dir, unresolved overwrite conflict, --force without --reason, undecided archive) now exits 2 via a new die() helper, not 1. regression_diff.py: 0/1 preserved for pass/genuine-field-divergence; added upfront exit-2 validation for missing --run-dir/--baseline-dir (including per-nproc subdirs), malformed --nprocs, and unreadable --tolerance-file; a candidate/baseline .nc file that fails to open (corrupt/wrong format) is now caught and reported as a FAIL result (exit 1, 'unreadable' status with detail) instead of an uncaught traceback -- a bad output file is a regression, not a tool misconfiguration.
- (event) Re-tested both scripts end-to-end after the exit-code refactor: manage_baseline.py's normal promote (0), missing-run-dir (2), unresolved-conflict (2), and force-without-reason (2) all correct. regression_diff.py's normal pass (0), missing-baseline-dir (2), malformed --nprocs (2), unreadable --tolerance-file (2), and a corrupt candidate NetCDF file (1, reported cleanly as an 'unreadable' FAIL rather than a Python traceback) all correct. Full prior 4-scenario suite (exact/gpu-within-tol/gpu-exceeds-tol/missing-var) re-verified unaffected via --json pass field, after an initial false alarm from a shell test-harness bug (piping through tail masked the real exit code with tail's) -- not a bug in the tool itself.
- (decision) Fixed I/O timing instrumentation naming convention (FR-3/FR-33), confirmed by regcm5-dev: io_gather_<path>, io_write_<path>, io_wait_<path>, where <path> in {hist, rst, clm} (diagnostic/history mod_ncout.F90 FR-5; restart/checkpoint mod_savefile.F90; CLM land-model history). Verified against the real API in Main/mpplib/mod_service.F90:165-183 -- time_begin(name, indx)/time_end(name_of_section, indx, isize) take a free-form character(len=64) label plus a persistent per-call-site integer index (dbgslen=64), not a fixed enum/macro, so this is purely a naming-discipline convention, not a language constraint. Debug output prints only name(1:20), so io_gather_hist/io_write_hist/io_wait_hist etc. were chosen to read clearly within that 20-char truncation. This lets FR-34's cross-path I/O report (diagnostic vs restart vs CLM) aggregate gather/write/wait time uniformly instead of each path inventing its own ad hoc label.
- (constraint) Scope note: only the naming CONVENTION was fixed and logged here, not the FR-3/FR-33 instrumentation itself -- actually adding time_begin/time_end calls to Main/mpplib/mod_ncout.F90, Main/mod_savefile.F90, and CLM's history module is a real Fortran source change to the model (needs a compiler build + the project's full Build/Test/Numerical/Performance acceptance record), qualitatively bigger/riskier than the Python tooling built so far in this session. Deliberately not done without an explicit go-ahead; flagged rather than silently expanded into.
- (decision) Resolves the redistribution-licensing half of PRD Open Question 7 (FR-30's NVHPC/CUDA container variant), web-verified against NVIDIA's official HPC SDK Container Guide 26.3 (matching our pinned NVHPC target): NVIDIA provides two container types -- 'devel' (full SDK, not freely redistributable) and 'runtime' (only the components NVIDIA licenses for redistribution). Sanctioned pattern is a multi-stage Docker/Apptainer build: compile RegCM5 against the devel image, then copy only the compiled binary and its runtime dependencies into the runtime-stage image for distribution -- never ship the full devel SDK. FR-30's container build must follow this devel-build/runtime-ship split, not a single-stage image bundling the whole HPC SDK. The other half of Open Question 7 (whether to provision a self-hosted GPU-capable GitHub Actions runner) remains a regcm5-dev decision, not something web research resolves -- still open.
- (question) PRD Open Question 7 (self-hosted GPU CI runner): still open, unresolved by this session -- requires regcm5-dev's decision on infrastructure/account scope, not research. Listed under Deferred in the spine.
- (event) ARCHITECTURE-SPINE.md distilled (18 initial ADs) by a subagent from the full 44-entry memlog; lint_spine.py passed clean (0 findings) on first draft.
- (event) 5 parallel background reviewer-gate subagents (2 reconciliation, rubric walker, web-verification, adversarial-incompatibility) all failed simultaneously on a shared session-limit API error before producing output. Per the reviewer-gate's documented fallback ('if subagents are unavailable, run sequentially'), all 5 reviews were completed inline by the orchestrating session instead, each written to reviews/review-*.md.
- (decision) Reviewer-gate fixes applied to the spine: (1) added AD-19, boundary-condition guardrail extending to FR-34's I/O-scaling work -- confirmed missing by both PRD and project-context.md reconciliation independently; (2) AD-1 gained an explicit tolerance-table growth rule (a newly GPU-ported field not yet in the table defaults to bit-exact, not a bug) -- the adversarial lens's highest-value finding, a silent-divergence risk between two people's assumptions about AD-1's scope vs. regression_diff.py's actual DEFAULT_GPU_TOLERANCES; (3) AD-7's Binds corrected to drop an erroneous FR-12 citation (FR-12 is GPU target-architecture configurability, unrelated to the managed/stdpar memory-mode choice AD-7 governs); (4) AD-17 now restates the DEBUG-macro-gating/zero-non-DEBUG-overhead constraint, confirmed relevant by both the PRD's Cross-Cutting NFRs and project-context.md's Debug facility rules; (5) Capability Map gained a 4.9 GitHub CI/CD row (AD-4/AD-16 already bound its FRs but the map omitted it) and a 4.5/FR-20 footnote (quiet prerequisite for AD-14/AD-15's vendor-distinct build matrix); (6) Consistency Conventions gained a caveat about manage_baseline.py log silently reporting a false 'no manifest' negative if --nproc is forgotten on a per-process-count baseline. Re-verified --nproc symmetry across --run-dir/--baseline-dir/--archive-dir directly against the code -- already correct, no fix needed there.
- (decision) Two genuine open questions surfaced by the adversarial lens were added to Deferred rather than silently resolved: (1) container base OS/distro is undecided across the GNU/Intel/NVHPC container variants -- a real FR-28 bit-exact-agreement risk if left to three independent builders; (2) no default OpenACC memory mode (managed vs stdpar) is designated for actual GPU production/profiling runs on boost_usr_prod, only for build-matrix testing coverage (AD-7). Both need regcm5-dev's decision, not research or invention.
- (event) lint_spine.py re-run clean (0 findings) after all reviewer-gate fixes applied. Spine now carries 19 ADs.
- (event) Companion architecture doc (ARCHITECTURE.md) drafted, structurally reviewed (1 CONDENSE applied to Open Items, 1 CLI-detail question resolved as PRESERVE), and prose-reviewed (4 ambiguous-reference/clarity fixes applied: file-to-switch mapping, $FAST area wording, an ambiguous 'it', an ambiguous 'them'). Both doc_standards lenses (bmad-editorial-review-structure, bmad-editorial-review-prose) run per customize.toml.
- (event) spine finalized
- (event) PAUSED this architecture run to correct prd-RegCM-2026-07-04 directly: discovered during coaching that the PRD's Feature 4.3 'four confirmed hotspots' premise is substantially incomplete. git grep found !$acc directives in 90 files (1,476 lines total) -- not 4 -- including a real GPU-direct halo-exchange implementation in Main/mpplib/mod_mppparam.F90 (commit b186b1a91 '4d exchange implemented on GPU') that directly contradicts FR-11's 'investigation only, no implementation committed' framing, and real !$acc kernels/parallel loop directives already in Main/radlib/mod_rad_colmod3.F90 (RRTMG-adjacent) that contradict FR-9's premise RRTMG is entirely unmeasured/unported. Git history shows 34+ commits from named NVIDIA engineers (Everett Phillips, Massimiliano Fatica) spanning 2025-04-23 through 2026-06-08+, across MOLOCH, the full CLM4.5 land model, cloud/cumulus/microphysics, ocean, PBL, OASIS coupling, and async pinned-buffer I/O -- sustained real engineering, not stale/experimental code. regcm5-dev chose to correct the PRD now rather than defer. Resuming bmad-prd in update mode.
- (event) RESUMED after correcting prd-RegCM-2026-07-04 (FR-11 rescoped from 'investigation only' to 'verify existing implementation'; FR-35 rescoped to full 90-file footprint). This spine's Deferred entry for halo-exchange GPU-direct is now factually wrong and must be corrected, not just re-worded.
- (decision) Precise code verification (Main/mpplib/mod_mppparam.F90): GPU-direct halo exchange is genuinely implemented, not hypothetical. The real8_2d_exchange/real8_3d_exchange/real8_4d_exchange routine family (and directional variants: _left_right, _bottom_top, _left_right_bottom_top, etc. -- 51 total host_data use_device occurrences file-wide) pack boundary data into contiguous scratch buffers via do-concurrent loops inside an !$acc data copy(ml) create(sdatax,sdatay,rdatax,rdatay) region, then wrap the actual mpi_isend/mpi_irecv calls in !$acc host_data use_device(...) to pass device pointers directly (CUDA-aware MPI / GPUDirect RDMA). This is entirely internal to each routine body -- callers see no signature change. By contrast, the simpler point-to-point exchange_array_r8/r4 primitives (Main/mpplib/mod_mppparam.F90:1161-1203) remain plain host-memory MPI calls with zero acc directives -- GPU-direct is scoped to the packed multi-dimensional exchange family specifically, not universal across every communication primitive in the file. This directly confirms (not merely permits) the original coaching answer's instinct: GPU-direct as a drop-in behind existing routine signatures is exactly the pattern already implemented and regcm5-dev-confirmed as scientifically validated.
- (change) Corrected both architecture artifacts for the PRD's FR-11 rescoping: added AD-20 (GPU-direct halo exchange implemented, not hypothetical -- real8_2d/3d/4d_exchange family, 51 host_data use_device occurrences, do-concurrent pack/unpack inside acc data regions, no caller-visible signature change) to ARCHITECTURE-SPINE.md, added it to the Capability Map's 4.3 row, and removed the now-false Deferred entry. Rewrote ARCHITECTURE.md section 3's halo-exchange paragraph to match. lint_spine.py re-confirmed clean (0 findings), spine now carries 20 ADs.
- (decision) PRD Open Question 7 (self-hosted GPU CI runner), the last open item from this architecture session, resolved by regcm5-dev during a later bmad-help status check: no self-hosted GPU-capable GitHub Actions runner will be provisioned. Feature 4.9's triggered CI remains permanently scoped to CPU-path builds/tests on GitHub-hosted runners; GPU-path validation stays exclusively on the HPC cluster's Slurm-based workflow, matching PRD §5 Non-Goals as already written -- no Non-Goals rewording needed, just removal of the open-question framing. Updated prd.md (Open Question 7 struck through as resolved, §9 Assumptions Index note updated) and ARCHITECTURE-SPINE.md (removed the now-resolved Deferred entry; no new AD needed since this confirms existing scope rather than adding build guidance).
- (decision) Resolves Deferred item: container base OS/distro across all three variants (GNU, Intel, NVHPC/CUDA) is Ubuntu 24.04. Web-verified: NVIDIA's HPC SDK 26.3 (our pinned target) publishes an Ubuntu 24.04 devel tag (nvcr.io/nvidia/nvhpc:26.3-devel-cuda_multi-ubuntu24.04, confirmed via nvcr.io mirror), so this doesn't conflict with AD-18's devel/runtime multi-stage pattern -- HPC SDK Ubuntu 24.04 support began around v24.7. All three container variants (FR-28 GNU, FR-28 Intel, FR-30 NVHPC) build from Ubuntu 24.04 base images, resolving the bit-exact-agreement risk AD-18/Deferred flagged from three independent builders each picking a different distro.
- (decision) Resolves Deferred item: default OpenACC memory mode for GPU production/profiling runs on boost_usr_prod is explicit device memory allocation -- i.e. configure.ac's plain --enable-openacc (USE_OPENACC=1: -acc=gpu -gpu=$PGI_GPU_ARCH,lineinfo -Minfo=accel, no mem:managed/mem:unified flag), requiring explicit !$acc data/copyin/copyout/create clauses rather than CUDA managed or unified memory. Grounded in real code evidence gathered during the PRD correction: the broader validated GPU-porting footprint already uses this pattern -- AD-20's halo-exchange routines use !$acc data copy(ml) create(sdatax,sdatay,rdatax,rdatay), and the meanall_1D_real8/real4 reduction routines use !$acc data copyin(...)/copyout(...) -- both explicit management, not automatic. This is consistent with, not a risky departure from, the codebase's actual current direction. Does not affect AD-8's canonical baseline (Intel/GNU partition, no GPU).
- (change) Both remaining Deferred items resolved into AD-21 (container base OS: Ubuntu 24.04, all three variants, web-verified compatible with AD-18's NVHPC devel/runtime pattern) and AD-22 (default OpenACC production/profiling memory mode: explicit device memory allocation, plain --enable-openacc, grounded in AD-20's and meanall_1D_real8/real4's existing explicit-management pattern). Applied to both ARCHITECTURE-SPINE.md (now 22 ADs) and ARCHITECTURE.md (sections 9 and 11). lint_spine.py clean.
- (event) spine finalized (update: AD-21, AD-22 added, resolving the last two open Deferred items)
