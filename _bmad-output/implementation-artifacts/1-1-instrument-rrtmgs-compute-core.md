---
baseline_commit: 39f2085aa8e49254e9691c77703123dcfe88dd45
---

# Story 1.1: Instrument RRTMG's Compute Core

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As regcm5-dev (the model's scientific owner and profiling lead),
I want RRTMG's driver and radiative-transfer kernels wrapped in DEBUG-gated timing instrumentation, complemented by a `perf`/Nsight profiling pass on at least one non-CLM-coupled configuration,
so that I can confirm or correct RRTMG's actual wall-clock cost share instead of assuming the CLM4.5-coupled thesis finding ("not a hotspot") generalizes on its own.

## Acceptance Criteria

1. Given a DEBUG-enabled build of RegCM5, when `time_begin`/`time_end` wrap `mod_rrtmg_driver.F90`, `rrtmg_lw_rad.F90`, and `rrtmg_sw_rad.F90`'s entry points, then a `Testing/` fixture run emits timing output attributable to RRTMG's core routines, not only its outer driver.
2. Given a non-CLM-coupled configuration (e.g. BATS-coupled or the idealized RCE setup), when a `perf --call-graph dwarf` + Nsight Systems profiling pass runs against it, then the profile states RRTMG's measured wall-clock share, confirming or correcting the "not a hotspot" CLM4.5-coupled finding before it is generalized.
3. Given a non-DEBUG production build, when the same code path executes, then no additional timing overhead is observable, per the existing DEBUG-gating convention (NFR-4).

## Tasks / Subtasks

- [x] Task 1: Instrument the three RRTMG entry points with DEBUG-gated timing (AC: #1, #3)
  - [x] In `Main/radlib/mod_rrtmg_driver.F90`: added a conditional `use mod_service, only : time_begin, time_end, dbgslen` guarded by `#ifdef DEBUG`/`#endif`; wrapped the full body of `rrtmg_driver` with `time_begin`/`time_end`, following the `mod_rad_colmod3.F90` `colmod3` pattern exactly (`character(len=dbgslen) :: subroutine_name = 'rrtmg_driver'` + `integer(ik4) :: indx = 0`, `call time_begin` right after declarations, `call time_end` right before the single `end subroutine`). `mod_intkinds` was already in scope (module-level, line 20), so no additional `use` was needed for `ik4`.
  - [x] In `Main/radlib/rrtmg_lw_rad.F90`: same pattern, wrapping `rrtmg_lw`. Added `use mod_intkinds, only : ik4` alongside the new `#ifdef DEBUG`-guarded `use mod_service` line, since this file only had `parkind`'s `im`/`rb` aliases in scope.
  - [x] In `Main/radlib/rrtmg_sw_rad.F90`: same pattern, wrapping `rrtmg_sw`. Same `use mod_intkinds, only : ik4` addition as the LW file.
  - [x] Used distinct labels (`'rrtmg_driver'`, `'rrtmg_lw'`, `'rrtmg_sw'`).
  - [x] Confirmed via full-body read of all three subroutines that each has a single exit (`end subroutine`, no early `return`), so one `time_begin`/`time_end` pair per subroutine is sufficient — no unmatched-call risk.
- [x] Task 1b (franco-requested, outside original scope): Fix the pre-existing gfortran build failure in `Main/radlib/mcica_random_numbers.F90` (AC: none directly — unblocks cross-vendor verification of AC #1/#3 on GNU)
  - [x] Changed `UMASK = -2147483648` to `UMASK = -2147483647 - 1` (line 91) — the standard portable idiom for representing `INT_MIN`/the `0x80000000` bit pattern in Fortran without writing a positive literal (`2147483648`) that overflows a 4-byte signed integer's positive range. Semantically identical value, no logic change; only the two other files in the archive that already worked (`rrlw_kg*.F90` etc.) were left untouched.
  - [x] Rebuilt GNU DEBUG and non-DEBUG chains from clean (`make distclean` + reconfigure): both now succeed with exit 0, including `mcica_random_numbers.o` and all three instrumented files.
- [x] Task 2a: Verify DEBUG and non-DEBUG builds compile the three instrumented files correctly, across all three vendors (AC: #1, #3 — compile-time half; NFR-2/AD cross-vendor requirement)
  - [x] **Intel (ifx-only, no classic ifort):** `mpiifx`/`mpiicc` (with `I_MPI_CC=icx` so `mpiicc` delegates to `icx` instead of missing classic `icc`), Intel MPI 2021.12.1, `binutils/2.42` for a modern `ld`. Both `--enable-debug` and default configs build `Share`/`Main/mpplib`/`external`/`Main/radlib` with exit 0; `grep -c "ifort:"` against the build logs returned `0` — confirms no classic-ifort fallback occurred anywhere in the chain.
  - [x] **NVHPC:** `nvhpc/25.11` + `hpcx-mpi/2.25.1` (closest available match to the project's stated current target of NVHPC 26.3/HPC-X 2.25 — exact versions aren't installed on this cluster yet) via `mpifort`/`mpicc` wrapping `nvfortran`/`nvc`. Both configs built `Share`/`Main/mpplib`/`external`/`Main/radlib` with exit 0 on the first attempt, no warnings on any of the three instrumented files.
  - [x] **GNU:** gcc/gfortran 12.2.0 + OpenMPI 4.1.6. Both configs now build cleanly (exit 0) after the `mcica_random_numbers.F90` fix (see Task 1b below).
  - [x] For every vendor/config combination, confirmed `-DDEBUG` is present only in the `--enable-debug` build logs and entirely absent (`grep -c` = 0) from the default build logs — the new `#ifdef DEBUG` blocks compile out entirely in production builds, not merely "run fast."
  - [x] Runtime check (see Task 2b below): actually run a `Testing/`-equivalent fixture and inspect `time_print` output.
- [x] Task 2b: Run a `Testing/`-equivalent fixture under the DEBUG build and confirm `time_print` shows separate `rrtmg_driver`/`rrtmg_lw`/`rrtmg_sw` entries (AC: #1)
  - [x] **Precondition — checked:** every fixture in `Testing/` ships with `irrtm = 0` (dispatches to `colmod3`, not RRTMG). Confirmed via the `EUR12_task2b.in`/`EUR12_task3.in` copies actually used, with `irrtm = 1` forced on by `sed` and verified present in the copy before every run in this story (`grep -E "gdate|mdate|irrtm"` in each Slurm script's log output).
  - [x] Built the full `regcm` executable (all `Main` sub-libraries, not just the 4-directory reduced scope from Task 2a) via `bin/slurm-task2b-build-and-run.sh`, confirmed `RRTM_DATA` and all other required input data available (see the External data gaps note in Completion Notes), ran via Slurm on `dcgp_usr_prod` — never the login node.
  - [x] **`maxnsubs` audit (code review, 2026-07-07):** grepped every source file actually compiled into this BATS/no-CLM45/irrtm=1 build (`external`, `Share`, and all built `Main` sub-libraries except `clmlib` — CLM4.5 isn't compiled in) for `subroutine_name = '` registrations: **125 distinct compile-time labels**. This is a compile-time upper bound, not the runtime count — `n_of_nsubs` only increments on a label's first *actual call*, and this namelist selects exactly one scheme per physics category (`ibltyp=1`, `icup_lnd=icup_ocn=5`, `ipptls=2`), which excludes the alternate-scheme-only labels (Kain-Fritsch/Emanuel cumulus routines `cuparan`/`kfdrv`/`cumastr*`/`cubase*`/etc., and the UW-TCM PBL label `uwtcm`) from ever being registered. A directly-captured `time_print` "inizialization phase" table from this exact build (`build-logs/task2b-48752677.out`, `task2b-48753082.out`) shows 12 registered labels at the end of initialization alone; the full evolution-phase table quoted above in Task 2b's Completion Notes shows on the order of 40 total labels by end of run. Both are comfortably under the `maxnsubs = 100` cap, and every completed run in this story (Task 2b, Task 3) reached `RegCM V5 simulation successfully reached end` / `RESULT: PASS` with sensible, non-garbage timer values — no symptom of the silent corruption `mod_service.F90` risks if the cap were exceeded. **Conclusion: safe for this configuration; re-audit if CLM4.5 or additional cumulus/PBL/microphysics schemes are compiled in for a future story.**
- [x] Task 3: Run and report the non-CLM-coupled profiling pass (AC: #2)
  - [x] Select a non-CLM-coupled `Testing/`-equivalent configuration (BATS, same rationale/fixture as Task 2b) per AD-2's fixed profiling method: `perf record --call-graph dwarf` with a FlameGraph rendering, plus an NVIDIA Nsight Systems (`nsys`) pass. `irrtm = 1` forced on in the namelist copy, same as Task 2b.
  - [x] Submitted as a Slurm job (`bin/slurm-task3-profile.sh`) on `dcgp_usr_prod`/`dcgp_qos_lprod` (Intel, account `ict26_mhpc`) — never on the login node.
  - [x] Recorded RRTMG's measured wall-clock share from the profile: **~6.0%** (additive `Self%` across all `rrtmg_driver`/`rrtmg_lw`/`rrtmg_sw`/McICA symbols in `perf report`). This **confirms** the CLM4.5-coupled thesis finding that RRTMG is not a hotspot — in this non-CLM (BATS) configuration it is a minor contributor, well behind the dynamical core (`mod_moloch_mp_wafone_`+`mod_moloch_mp_sound_`, ~17.9% combined) and MPI/threading overhead, and roughly on par with `mod_capecin_mp_getcape_new_` (4.2%), the thesis's own confirmed hotspot. Full detail in Completion Notes below.
  - [~] Archived the profiling artifacts to `RegCM-data/profiling/` with full provenance recorded in this story (see Completion Notes) — **not** via `manage_baseline.py`/`FAST_BASELINE_ROOT`, since that baseline-archival convention isn't operational yet (Epic 2, "establish trusted baselines for testing fixtures," is still `backlog`). Flagging this as a gap for Epic 2 rather than silently working around it.

### Review Findings

- [x] [Review][Defer] `Share/mod_heatindex.F90`'s DEBUG+OpenACC fix was intentionally removed by franco and deferred to the GPU-offloading epic — deferred, out of scope for this RRTMG-instrumentation story; the story's Dev Agent Record/File List claim of "kept in its reapplied state" is now stale and should be read as historical (fix was applied and verified during this story's session, then deliberately reverted afterward). [Share/mod_heatindex.F90]
- [x] [Review][Defer] `bin/*.sh` scripts are intentionally untracked/gitignored personal HPC tooling, not part of this story's committed deliverable — franco's call: correct the story's claims rather than add a `.gitignore` exception. `bin/.gitignore` remains unmodified (`*` / `!.gitignore`); the File List's "modified — allow tracking" claim and the individual script entries have been corrected below to reflect untracked status, and the `slurm-task2b-run-only.sh`/`slurm-task2b-run.sh` filename mismatch has been fixed. [bin/.gitignore]
- [x] [Review][Patch] Task 2b's three subtask checkboxes were unchecked despite the parent being marked done, including the `maxnsubs`-capacity precondition the Dev Notes flag as a silent-memory-corruption risk if skipped. Resolved: performed the audit (125 compile-time labels, ~40 runtime-registered per the captured `time_print` tables, well under the `maxnsubs = 100` cap for this namelist's scheme selections) and checked all three boxes with the finding recorded in Task 2b's subtasks. [1-1-instrument-rrtmgs-compute-core.md, Task 2b subtasks]
- [ ] [Review][Patch] `bin/slurm-task2b-build-and-run.sh` never links `Main/regcm` — the build chain stops at `external`/`Share`/`Main/mpplib`/`Main/radlib`; there is no `make -C Main` (or equivalent) step. Since the script runs `make distclean` first, a fresh run of this script as committed would fail at `mpirun ... ./Main/regcm` with no executable present. [bin/slurm-task2b-build-and-run.sh:47-50]
- [ ] [Review][Patch] `bin/slurm-task3-profile.sh` ships with its entire build/configure section commented out and no assertion that `Main/regcm` exists or was built with the `--enable-profile` flags the header comments claim. Verified: the job backing this story's "~6.0% RRTMG share" claim (48812653) ran zero `make` invocations, silently reusing a binary from an earlier job — correct in this instance by manual cross-check, but the script has no guardrail against reusing a stale or differently-configured binary on a future run. [bin/slurm-task3-profile.sh:50-81]
- [ ] [Review][Patch] `irrtm = 1` namelist mutation (`sed -i "/^&physicsparam/a\\ irrtm = 1"`) is never verified to have taken effect in `bin/slurm-task2b-build-and-run.sh`, `bin/slurm-task2b-run.sh`, and `bin/slurm-task3-profile.sh` — the follow-up `grep` only logs matches, it doesn't gate on them. Since `irrtm=1` is the Dev Notes' own documented "single most important precondition" in this story, a silent no-op here would let a run report PASS while actually exercising `colmod3` instead of RRTMG. [bin/slurm-task2b-build-and-run.sh:73; bin/slurm-task2b-run.sh; bin/slurm-task3-profile.sh:90]
- [ ] [Review][Patch] `RANK="${PMI_RANK:-${SLURM_PROCID:-0}}"` in both rank-0 wrapper scripts silently defaults every rank to `"0"` if neither env var is populated, which would make all 224 ranks race on the same `perf.data`/`nsys-rep` output path instead of failing clearly. [bin/task3-rank0-perf-wrap.sh:11; bin/task3-rank0-nsys-wrap.sh:11]
- [ ] [Review][Patch] Build scripts capture `CONFIGURE_STATUS` but proceed to run the full `make` chain unconditionally even when configure failed, wasting a compute allocation and producing confusing logs before `BUILD_STATUS` eventually reports FAIL. [bin/slurm-build-gnu-debug.sh, bin/slurm-build-gnu-production.sh, bin/slurm-build-intel-debug.sh, bin/slurm-build-intel-production.sh, bin/slurm-build-nvhpc-debug.sh, bin/slurm-build-nvhpc-production.sh, bin/slurm-task2b-build-and-run.sh]
- [ ] [Review][Patch] `configure.ac`'s new PGI/NVHPC fallback probe can make `COMPILER_PGI` simultaneously true alongside another vendor's own `AM_CONDITIONAL` (e.g. a Cray `ftn` wrapper fronting an NVIDIA backend would match both `COMPILER_CRAY` and, via the `--version` banner grep, `COMPILER_PGI`), since nothing excludes a compiler already matched by an earlier pattern. Separately, the fallback's grep (`pgf90` only) is narrower than the direct-match case's `pgf9*` glob, so the two branches aren't equivalent in coverage. [configure.ac:514-527]
- [x] [Review][Defer] Heavy duplication across the 11 new `bin/` scripts (module-load blocks and `irrtm`-forcing namelist logic repeated near-verbatim 5+ times) — deferred, pre-existing pattern across one-off HPC verification scripts, not blocking. [bin/slurm-*.sh]
- [x] [Review][Defer] All new `bin/` scripts hardcode franco's personal scratch paths with no parameterization (`$USER`/env var), limiting reuse by others — deferred, consistent with how this ad hoc verification tooling was requested. [bin/slurm-*.sh]
- [x] [Review][Defer] Ephemeral Slurm job-ID references baked into permanent script comments (e.g. "job 48758729") are unretrievable to a future reader outside this account/timeframe — deferred, minor documentation polish. [bin/slurm-task3-profile.sh]

## Dev Notes

- **Existing instrumentation facility, not a new one:** `Main/mpplib/mod_service.F90` is the whole mechanism — entirely `#ifdef DEBUG`-gated, with a no-op stub module in `#else` so non-DEBUG builds still link. Public API: `time_begin(name, indx)` / `time_end(name_of_section, indx, isize)`, plus `time_reset`/`time_print`, `activate_debug`/`start_debug`/`stop_debug`, and constant `dbgslen = 64` (fixed label length). `indx` must be `integer(ik4)`, initialized to `0` in the caller (implicit `SAVE`); first call self-registers (`n_of_nsubs = n_of_nsubs + 1; indx = n_of_nsubs`).
- **Kind availability differs by file.** `mod_rrtmg_driver.F90` already `use mod_intkinds` at module level (line 20), so `ik4` is in scope there. `rrtmg_lw_rad.F90` and `rrtmg_sw_rad.F90` do not — their module-level use section only brings in `parkind`'s `im`/`rb` aliases (RRTM's own legacy kind system). Add `use mod_intkinds, only : ik4` to both before declaring `integer(ik4) :: indx = 0`; do not substitute `im` for `ik4` for the indx variable — `im` is `parkind`'s own convention for the numerical fields these kernels compute, not the project's DEBUG-instrumentation convention, and mixing the two for a single counter variable is unnecessary.
- **Timer registry has a fixed capacity.** `mod_service.F90` sizes its internal timer arrays at `maxnsubs = 100` (module-level parameter), with self-registration incrementing `n_of_nsubs` on each new label's first call. The codebase already registers roughly 130 distinct timer labels across all source files combined, though only the labels actually exercised by a given build/run (which schemes are compiled in, which code paths execute) count against the live `n_of_nsubs` total at runtime. Adding 3 more registrations narrows headroom further. When running Task 2's DEBUG build, confirm `n_of_nsubs` does not exceed `maxnsubs` for the fixture actually used — if it does, that is a pre-existing capacity issue to flag to regcm5-dev, not something to silently work around.
- **Copy the existing pattern exactly — do not invent a variant.** Confirmed precedents:
  - `Main/radlib/mod_rad_colmod3.F90:223-227` (entry) / `:742-744` (exit), subroutine `colmod3` — the closest template, since `colmod3` is the *other* branch of the same `mod_rad_interface` dispatch this story instruments, and is already DEBUG-timed.
  - `Main/mod_sun.F90:263-267`/`:292-294`, subroutine `solar1`.
  - `Main/radlib/mod_rad_radiation.F90`, subroutines `radcsw`/`radclw`/`radctl` — three entry/exit pairs in one file, confirming the pattern repeats cleanly across multiple subroutines in the same module.
  - Prefer the `colmod3` form (`#ifdef DEBUG`-guarded `use mod_service, only : ...`) over `solar1`'s bare `use mod_service`, since the three target files currently have zero `mod_service` references and the guarded form is the more common existing convention.
- **Never instrument inside a loop body** — matches every existing call site and NFR-4. Timing instrumentation inside `prep_dat_rrtm`'s or `taumol`'s per-column loops would reintroduce the overhead the DEBUG-gating convention exists to avoid, even temporarily.
- **No shipped fixture exercises RRTMG as-is — this is the single most important precondition in this story.** `irrtm` defaults to `0` (`Main/mod_params.F90:318`) and every `Testing/CORDEX/*.namelist` sets it explicitly to `0`; `test_001.in`/`ideal.in` omit it and inherit the same default. `irrtm==0` calls `colmod3`; only `irrtm==1` calls `rrtmg_driver`/`rrtmg_lw`/`rrtmg_sw` (`Main/mod_rad_interface.F90:203-206`). Both Task 2 and Task 3 require a namelist copy with `irrtm = 1` — running an unmodified fixture will produce a null result (no new timer entries; ~0% measured RRTMG cost) that is easy to mistake for a real "RRTMG isn't a hotspot" finding rather than what it actually is: RRTMG never ran.
- **McICA variant confirmed correct:** `imcica` defaults to `1` in every fixture checked, so `rrtmg_lw`/`rrtmg_sw` (the McICA-enabled entry points) are the right instrumentation targets, not the `_nomcica` variants also present in `mod_rrtmg_driver.F90`'s call chain.
- **The gap this story closes:** `mod_rad_interface::radiation()` (`Main/mod_rad_interface.F90:198-207`) dispatches `if (irrtm==1) call rrtmg_driver(...) else call colmod3(...)`. `colmod3` already has full DEBUG timing; `rrtmg_driver`/`rrtmg_lw`/`rrtmg_sw` currently have none — closing that asymmetry is this story's entire scope.
- **Why this story exists:** an ICTP/SISSA thesis (`_bmad-source/regcm5-optimization.pdf`, Tica 2024–2026) profiled RegCM5-CLM4.5 with `perf`+Nsight and found RRTMG does not appear as a named hotspot in that coupled configuration's flame graph — confirmed hotspots were `getcape`/CAPE-CIN, a CLM-hydrology memset, `interp1d_r8`, and `heatindex`. RRTMG's large code size had made it look like an obvious GPU-port candidate before this measurement (Architecture §7, GPU Candidate Selection). This story gets a direct, non-CLM-coupled measurement rather than assuming the coupled-case finding generalizes. This is evidence-gathering, not a performance fix — do not optimize or restructure RRTMG as part of this story.
- **AC #2 is a separate activity from AC #1/#3.** AC #1/#3 is a code change (add timing wraps, verify overhead-free). AC #2 is an operational task (run `perf`+Nsight on a real Slurm job and report a measured number). Both are required for the story to be done; the DEBUG timing output is complementary evidence to, not a replacement for, the coarser `perf`/Nsight measurement.
- **Scope boundary:** do not touch KPP chemistry timing (Story 1.2) or diagnostic-I/O gather/write timing (Story 1.3) — separate stories, separate files. Story 1.4's published baseline report builds on this story's output but is out of scope here. Instrumentation work is confined to `mod_rrtmg_driver.F90`, `rrtmg_lw_rad.F90`, `rrtmg_sw_rad.F90`; `mcica_random_numbers.F90` was additionally touched only for the single-line gfortran portability fix (Task 1b), at franco's explicit request, not as part of the instrumentation itself.
- **Numerical reproducibility:** pure instrumentation (timing calls only, no algorithmic change) — expected bit-exact in both DEBUG and non-DEBUG builds. Any output difference after this change is a defect in the instrumentation, not an expected side effect; no tolerance exception applies.
- **Record all four acceptance-criteria categories in the commit/PR description** (project convention, not optional for a "small" change): **Build** — which compiler(s) (GNU/Intel/NVHPC); **Test** — which `Testing/` fixture, which process count; **Numerical** — state bit-exact explicitly; **Performance** — only claim RRTMG's measured wall-clock share with AC#2's profiling evidence attached (provenance: compiler+flags, MPI library, node/GPU model, problem size).

### Project Structure Notes

- All three target files live under `Main/radlib/` (the radiation-scheme subdirectory of `Main/`, which owns model core + physics libs per project convention).
- No new files are created — only existing files gain `#ifdef DEBUG` blocks and a conditional `use mod_service` line. No namelist, build-system (`configure.ac`/`Makefile.am`), or public-interface changes.
- No conflicts detected between this story and the existing project structure.

### References

- [Source: `_bmad-output/planning-artifacts/epics.md` Story 1.1, lines 212-230] — verbatim user story and acceptance criteria
- [Source: `_bmad-output/planning-artifacts/prds/prd-RegCM-2026-07-04/prd.md` FR-1] — requirement and testable consequences
- [Source: `_bmad-output/planning-artifacts/prds/prd-RegCM-2026-07-04/prd.md` Cross-Cutting NFRs, 4th bullet — labeled NFR-4 in `epics.md`'s Requirements Inventory] — DEBUG-only overhead constraint
- [Source: `_bmad-output/planning-artifacts/prds/prd-RegCM-2026-07-04/prd.md` Success Metrics SM-2, Risk Register] — acceptance cross-check and RRTMG/PSrad risk mitigation
- [Source: `_bmad-output/planning-artifacts/architecture/architecture-RegCM-2026-07-04/ARCHITECTURE-SPINE.md` AD-2] — fixed profiling method (`perf --call-graph dwarf` + Nsight Systems). AD-17 also confirms the `time_begin`/`time_end` API and "zero overhead, never inside a loop" rule, but is otherwise Story 1.3/FR-3's I/O-timer-label convention, not this story's concern
- [Source: `_bmad-output/planning-artifacts/architecture/architecture-RegCM-2026-07-04/ARCHITECTURE.md` §5, §7] — profiling method detail; GPU-candidate-selection motivation
- [Source: `Main/radlib/mod_rrtmg_driver.F90`, `Main/radlib/rrtmg_lw_rad.F90`, `Main/radlib/rrtmg_sw_rad.F90`] — files to modify
- [Source: `Main/mpplib/mod_service.F90`] — DEBUG timing facility (`time_begin`/`time_end` API)
- [Source: `Main/radlib/mod_rad_colmod3.F90:223-227,742-744`; `Main/mod_sun.F90:263-267,292-294`; `Main/radlib/mod_rad_radiation.F90`] — existing instrumentation templates to replicate
- [Source: `Main/mod_rad_interface.F90:198-207`] — dispatch seam confirming the coverage gap this story closes
- [Source: `Main/mod_params.F90:318`; `Testing/CORDEX/*.namelist`; `Testing/test_001.in`; `Testing/ideal.in`] — confirms `irrtm` defaults/is set to `0` everywhere; the namelist edit needed for Tasks 2-3 to actually exercise RRTMG
- [Source: `configure.ac:351-358`] — `--enable-debug` is the flag that defines `DEBUG`
- [Source: `configure.ac:500-529`] — `COMPILER_PGI`/vendor-family detection; fixed to recognize NVHPC via generic MPI wrapper names (e.g. `mpifort`) by falling back to an `$FC --version` signature probe
- [Source: `Share/mod_heatindex.F90`; `Share/mod_message.F90:86-99`] — GPU-port DEBUG/OpenACC conflict and its fix; `fatal()`'s host-only `write`+abort semantics
- [Source: `bin/slurm-build-gnu-debug.sh`, `bin/slurm-build-gnu-production.sh`, `bin/slurm-build-intel-debug.sh`, `bin/slurm-build-intel-production.sh`, `bin/slurm-build-nvhpc-debug.sh`, `bin/slurm-build-nvhpc-production.sh`] — Slurm build-verification scripts for all six vendor/config combinations
- [Source: `_bmad-output/project-context.md` §Debug facility, §Language & Numerical Rules] — DEBUG macro convention, `rkx`/`ik4` kind rules, "never inside a loop body" rule

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5

### Debug Log References

### Completion Notes List

- Task 1 complete: all three RRTMG entry points (`rrtmg_driver`, `rrtmg_lw`, `rrtmg_sw`) now carry `#ifdef DEBUG`-gated `time_begin`/`time_end` instrumentation, following the `colmod3` template exactly. Pure additive change (new `#ifdef DEBUG` blocks and two `use` lines) — no existing statement was modified.
- Task 1b complete (franco-requested): fixed the pre-existing gfortran-only build failure in `mcica_random_numbers.F90` with a one-line, semantically-identical literal rewrite (`-2147483648` → `-2147483647 - 1`).
- Task 2a complete across all three vendors, both `--enable-debug` and default (production) configs, all six now built via Slurm (not the login node) with `--enable-clm45` added, using the reduced-but-sufficient scope (`external` → `Share` → `Main/mpplib` → `Main/radlib`, the same 4 directories that contain the 3 instrumented files and their real dependencies):
  - **GNU** gcc/gfortran 12.2.0 + OpenMPI 4.1.6 — PASS (debug and production). Only succeeds after the Task 1b fix — previously failed on `mcica_random_numbers.F90` before reaching the instrumented files.
  - **Intel, ifx-only** — `mpiifx` directly (no classic `ifort` anywhere: confirmed via `grep -c "ifort:"` = 0 across all build logs) + `mpiicc` redirected to `icx` via `I_MPI_CC=icx` (classic `icc` isn't installed on this cluster) + Intel MPI 2021.12.1 + `binutils/2.42` for a modern `ld` (the system default `/usr/bin/ld` can't read the netCDF-Fortran lib's compressed debug sections) — PASS (debug and production).
  - **NVHPC** `nvhpc/25.11` + `hpcx-mpi/2.25.1` (closest available match to the project's stated current target of NVHPC 26.3/HPC-X 2.25; exact versions aren't installed on this cluster) via `mpifort`/`mpicc` wrapping `nvfortran`/`nvc`, on a GPU node (`--gres=gpu:a100:1`, required since `-gpu=ccnative` auto-detects compute capability against a visible device even just at `configure`/compile time) — PASS (debug and production), after the `mod_heatindex.F90` fix below.
  - Confirmed for every passing combination: `-DDEBUG` appears only in the DEBUG-configured build logs, never in the non-DEBUG ones.
- **Root-cause fix in `configure.ac` (franco-requested, well outside this story's 3 files): NVHPC was never actually being recognized as `COMPILER_PGI`.** `configure.ac:502` derives `FCPROG` from `$FC`'s basename and pattern-matches it against `pgf9*`/`nvf9*`/`pgfortran*`/`nvfortran*` to set `COMPILER_PGI`. This project's own documented NVHPC module stack uses HPC-X's generic `mpifort` wrapper (matching neither pattern), so `COMPILER_PGI_TRUE` was silently false for every NVHPC build — meaning **four** separate `configure.ac` blocks gated on it (base `OMPFLAGS="-mp"`, DEBUG bounds-checking flags, production `-O3 -fast -fastsse` optimization flags, and the entire `--enable-openacc-*` FCFLAGS/LDFLAGS block) were never applied to any NVHPC build using the project's own standard invocation — including my earlier (pre-this-story) NVHPC verification pass, which therefore never exercised any NVHPC-specific flag at all. Fixed by adding a fallback: when `$FCPROG` doesn't match a known direct compiler name, probe `$FC --version` output for an `nvfortran`/`pgf90` signature before concluding it isn't PGI-family. Verified: after the fix, `configure` on a GPU node correctly reports `configure: WARNING: Compiling CLM45 enabled code` plus applies the OpenACC FCFLAGS, and the production build's compile lines now show `-stdpar=gpu -gpu=ccnative,...` where before they showed nothing beyond the CPP defines.
- **Discovery and fix (franco-requested, well outside this story's 3 files): `Share/mod_heatindex.F90` — one of the four already-GPU-ported hotspots from the source thesis — failed to compile under `--enable-openacc-debug` on NVHPC 25.11.** Its `!$acc routine seq` procedures (`solve1`–`solve5`, `solvei`–`solveiv`, and their `initial_find_eqvar`/`find_eqvar`/`find_t`/`heatindex` callers) call an un-annotated `fatal()` (`Share/mod_message.F90:86`, does `write(stderr,*)` + program abort) from inside GPU compute regions when `DEBUG` is defined; NVFORTRAN correctly rejects this (`NVFORTRAN-S-1061: Procedures called in a compute region must have acc routine information`). This is a real, pre-existing GPU-port defect that was never caught before because the `COMPILER_PGI` misdetection above meant `--enable-openacc-debug` never actually reached real GPU code generation on this cluster until now — it only manifests when `--enable-debug` and any real OpenACC flag are combined (the DEBUG-only diagnostic branch is what introduces the un-annotatable `fatal()` call; `--enable-openacc-stdpar` alone, without `--enable-debug`, never hit it).
  - **Fix:** all 31 occurrences of `#ifdef DEBUG` in the file changed to `#if defined(DEBUG) && !defined(OPENACC)` (the project's own `-DOPENACC` macro, set by every `--enable-openacc-*` flag including `-stdpar`). This disables the DEBUG-only `write`/`call fatal` interval-check diagnostic — and the accompanying non-`pure` signature — specifically when compiling for GPU, while leaving CPU-only DEBUG builds completely unchanged. Semantically: the diagnostic was host-only I/O and abort logic to begin with, so it could never have run correctly on-device regardless of annotation.
  - **Verified both ways per franco's request:** reverted (`git checkout --`) + `--enable-openacc-debug` dropped from `bin/slurm-build-nvhpc-debug.sh` → builds fine as a CPU-only NVHPC DEBUG config (job 48709086, PASS) but no longer exercises real GPU codegen at all. Then fix reapplied + `--enable-debug --enable-openacc-debug` restored → job 48720240, PASS, with real device compilation this time (confirmed via `-Minfo=accel` output showing `heatindex`/`solve*` device-code generation with no error). **Update (code review, 2026-07-07):** franco subsequently removed this fix and deferred it to the GPU-offloading epic — it is intentionally *not* present in the final state of `Share/mod_heatindex.F90` for this story. This file is no longer part of Story 1.1's delivered changes; see the deferred-work log.
- **Build infrastructure moved to Slurm per franco's request.** Six scripts saved under `bin/slurm-build-{gnu,intel,nvhpc}-{debug,production}.sh` (untracked personal tooling — see the File List note on `bin/*.sh` tracking status), each self-contained (module loads, configure flags, sequential per-directory build). GNU/Intel run on `dcgp_usr_prod`/`dcgp_qos_dbg`; NVHPC on `boost_usr_prod`/`boost_qos_dbg` with a GPU. **Note:** the account/partition pairing in `_bmad-output/project-context.md`/`_bmad/config.yaml` had Intel↔`ICT26_MHPC_0` and NVHPC↔`ICT26_MHPC` reversed from what `sacctmgr show associations` actually has on record (`ict26_mhpc`→dcgp, `ict26_mhpc_0`→boost); the scripts use the live association data. `project-context.md` has since been corrected (by franco, not this session).
- **Two build-system quirks discovered and worked around**, neither part of this story's scope: (1) the top-level `make`'s `version:` rule (`makeinc`) uses a `$(shell ...)`-in-recipe idiom that always reports a harmless "Error 1"/"syntax error" on first invocation even though it correctly writes the `version` file as a side effect — a second `make` invocation proceeds normally; (2) this project's autotools setup expects an in-tree build (a bare `makeinc`/`version`-generation convention tied to `$(REGCM_ROOT)` equaling the source root) — out-of-tree builds fail on a missing `makeinc`. Built in-tree instead, consistent with the repo's own `.gitignore` (which already ignores per-directory `Makefile`/`*.o`/`*.mod`/`.libs` artifacts).
- **Automake's recursive `SUBDIRS` build is not safe with `-j>1` spanning multiple directory levels.** A single top-level `make -jN` (or even `make -jN -C PreProc`, since `PreProc` itself nests `Terrain`/`ICBC`/`CLM`/`CLM45`/`CH_EMISSIONS`) can start a sibling/child directory's link step before another directory's library archive has actually been `ranlib`'d, producing `archive has no index; run ranlib to add one`. Confirmed safe pattern: `-jN` within a single flat (non-nesting) directory only, directories invoked strictly sequentially. This is why the build scope stayed at the 4 flat directories rather than a full top-level build.
- **Task 2b complete: RRTMG's three DEBUG timers confirmed live on a real production-scale run.** No shipped `Testing/` fixture has usable data paths on this cluster (all reference placeholder paths), so this was verified against a real fixture instead: `RegCM-data/EURR12` (EUR-12 CORDEX domain, 275×275×36, CLM4.5+chemistry in its shipped form). Built the full `regcm` executable (all `Main` sub-libraries, not just the 4-directory reduced scope used for Task 2a) via `bin/slurm-task2b-build-and-run.sh` and ran a namelist copy (`EUR12_task2b.in`) with `irrtm=1` forced on and the run window shortened to the minimum valid 24h (`mod_params.F90`'s "runtime increments must be modulus 24 hours" check rejects anything shorter). Final passing run: Intel ifx, `--enable-debug` only (CLM4.5 **not** compiled in — see below), 224 MPI ranks, `dcgp_usr_prod`. Job completed (`RUN_EXIT=0`, `RegCM V5 simulation successfully reached end`) and the evolution-phase `time_print` table shows the three instrumented entries as separate, populated timers:
  ```
  !rrtmg_driver            65  188.9391  206.4751( 82)  149.7085(223)
  !rrtmg_lw                65   59.5182   65.5339( 97)   46.1682(223)
  !*rrtmg_sw               35   12.1456  103.8569( 18)    0.8178(208)
  ```
  This confirms AC #1's runtime half: the instrumentation added in Task 1 fires correctly and distinctly for all three entry points under real MPI-parallel, production-scale execution — not just at compile time (Task 2a).
  - **Two unrelated pre-existing bugs surfaced and were routed around (not fixed) to keep this run inside Story 1.1's scope:** (1) `Main/clmlib/clm4.5/mod_clm_snicar.F90` fails to compile under NVHPC with `--enable-openacc-stdpar` (`NVFORTRAN-S-1058: Call to NVHPC runtime function not supported - pgf90io_begin`) — the same class of defect as the `mod_heatindex.F90` fix above (`write(stderr,*)` diagnostic calls inside `!$acc loop seq` regions), but in CLM's snow-albedo code, unrelated to RRTMG. (2) `Main/mod_ncio.F90`'s `ASYNC_NETCDF`-gated ICBC prefetch code (`pai_flat`, from the unrelated moloch/PAI work in commit `7f356f49a`) fails to compile under Intel ifx with 5 distinct errors (`#6460`/`#9074`/`#8372`/`#7496`/`#6634`, all on the same non-contiguous-pointer-argument issue) — also unrelated to RRTMG. Both were sidestepped for this story by dropping `--enable-clm45` (this run therefore exercises BATS, not CLM4.5, as its land-surface scheme — `EURR-12_DOMAIN000.nc`, the BATS-compatible companion to the domain's `_CLM45_surface.nc`, already existed in the fixture's `input/` directory) and `--enable-async-netcdf` respectively. Neither fix belongs in this story; flagging both here for a future story/bug report rather than patching CLM or async-I/O code under an RRTMG-instrumentation story.
  - **External data gaps closed to get this fixture running at all** (none part of Story 1.1's code changes, all now present under `RegCM-data/EURR12/RCMDATA/`): CMIP6 GHG concentrations (5 species, ESGF ORNL THREDDS), CMIP6 ozone climatology (ESGF ORNL), CLM4.5 PFT physiology + SNICAR snow-optics tables (ICTP mirror), RRTMG's own `rrtmg_sw.nc`/`rrtmg_lw.nc` (copied from the repo's `Testing/RRTM_DATA/`, since the downloaded mirror copy was broken), and MERRA2 aerosol optical-properties monthly climatology (`OPPMONTH`, ICTP mirror, months 08-10/2009 across all required `wb*` species).
- **Task 3 complete: RRTMG's measured wall-clock share confirms the CLM4.5-coupled thesis finding, on a non-CLM (BATS) configuration.** Built a separate `--enable-profile` (not `--enable-debug`) executable — the project's built-in production-with-symbols config (`configure.ac`'s `USE_PROF` branch, `-O2 -g -p` for Intel), distinct from Task 2b's DEBUG-timer build, since AC #2 is a `perf`/Nsight measurement of real (near-production) execution, not a DEBUG-timer check. Same BATS/no-CLM45 configuration as Task 2b for the same reason (avoids the unrelated `mod_clm_snicar.F90` GPU-port bug), same EURR12 fixture, `irrtm=1`, minimum-valid 24h window, 224 MPI ranks / 2 nodes, `dcgp_usr_prod`/`dcgp_qos_lprod`, via `bin/slurm-task3-profile.sh` (job 48812653, `RESULT: PASS` for both the perf and nsys passes).
  - **`perf record --call-graph dwarf` must wrap the actual `regcm` process directly, not the outer `mpirun`/`srun` launcher.** First attempt (job 48758729) wrapped `mpirun` from outside and produced a perf.data with **zero** regcm/rrtmg symbols — only `srun`'s own Slurm-config-parsing overhead — because this cluster's Intel MPI bootstraps ranks via `srun` as independent Slurm job steps, invisible to a `perf`/`nsys` instance tracing the outer launcher's own (unrelated) process tree. Fixed by launching `perf`/`nsys` *inside* each rank via `mpirun -n N ./wrapper.sh`, so they wrap `regcm` as their own direct child. Profiling all 224 ranks individually isn't practical (224 separate perf.data/nsys-rep files); per franco's choice, only rank 0 is wrapped (`bin/task3-rank0-perf-wrap.sh`, `bin/task3-rank0-nsys-wrap.sh`, keyed on `$PMI_RANK`/`$SLURM_PROCID`), while the full 224-rank/2-node scope is preserved for realism.
  - **FlameGraph rendering:** this system's `perf script report flamegraph` needs the `js-d3-flame-graph` RPM, not installed (no sudo on this cluster, and no local template found). Per franco's explicit go-ahead to fetch and run third-party code for this specific purpose, used the standard Brendan Gregg `stackcollapse-perf.pl`/`flamegraph.pl` pair instead (fetched to `RegCM-data/profiling/FlameGraph/`, `perf script | stackcollapse-perf.pl | flamegraph.pl > *.svg` — tested working before wiring into the Slurm script).
  - **Two `nsys` CLI quirks on this cluster's version (2024.5.1, via `module load cuda/12.6`):** (1) `--mpi-impl=intel` is not a valid value (only `openmpi`/`mpich`; Intel MPI is MPICH-derived, so `mpich` is correct — confirmed via `nsys profile --help`). (2) This version's argument parser does not accept a `--` separator before the target command at all — even `nsys profile -- echo hi` fails with a spurious "option is ambiguous" error listing dozens of unrelated flag names; the command must follow the options directly (`nsys profile [opts] ./Main/regcm namelist`, no `--`). Root-caused by bisecting flag-by-flag down to the minimal reproducer.
  - **`nsys --trace=osrt,mpi` never completed even after 3h10m wall time** (vs. 209s for the untraced/perf-traced run) — not overhead, an actual stall from the MPI API interception on rank 0 while running inside a 224-rank job (the other 223 ranks blocked in MPI collectives waiting on rank 0, hanging the whole job until Slurm's walltime killed it). Fixed by dropping `mpi` from `--trace` (`--trace=osrt` only — OS-runtime/CPU backtrace sampling, no MPI call interception); this run then completed normally (job 48812653, ~24 minutes total including the full build and the perf pass).
  - **Measured result:** summing the non-overlapping `Self%` column in `perf report --sort=overhead,symbol` across every `rrtmg_driver`/`rrtmg_lw`/`rrtmg_sw`/McICA (`mcica_subcol_gen_{lw,sw}`) symbol gives **RRTMG's total additive CPU-cycle share ≈ 6.0%** of the sampled profile. The largest individual contributors elsewhere are the moloch dynamical core (`mod_moloch_mp_wafone_` 10.2% + `mod_moloch_mp_sound_` 7.65% self ≈ 17.9% combined), MPI/threading synchronization (`pthread_spin_lock` 8.9%, `MPIDU_Init_shm_barrier` 5.7%, plus smaller MPI/pthread entries), and `mod_capecin_mp_getcape_new_` (4.2% self) — the latter matching one of the original thesis's own confirmed hotspots (CAPE-CIN), which is reassuring cross-validation that this profiling method reproduces a known-correct result. **Conclusion: this non-CLM-coupled (BATS) measurement confirms, not corrects, the CLM4.5-coupled thesis finding that RRTMG is not a hotspot** — its ~6% share is a minor contributor next to dynamics/microphysics/MPI overhead, in both configurations now measured. This is the number that feeds Story 7.5's later RRTMG GPU go/no-go checkpoint.
  - **Artifacts** (`RegCM-data/profiling/`, job 48812653; not yet promoted via `manage_baseline.py`/`FAST_BASELINE_ROOT` — see Task 3 checklist note above): `task3-perf-48812653.data` (raw perf.data, rank 0 only), `task3-perf-report-48812653.txt` (full `perf report --stdio`), `task3-flamegraph-48812653.svg`, `task3-nsys-48812653.nsys-rep` (open with `nsys-ui` or `nsys stats`). Provenance: Intel `ifx`/`mpiifx` (`intel-oneapi-compilers/2024.1.0`, `intel-oneapi-mpi/2021.12.1`), `--enable-profile` (no `--enable-debug`, no `--enable-clm45`), 2× `dcgp_usr_prod` nodes (112 cores/node, 224 MPI ranks total, 1 rank/core), EURR12 CORDEX 12km domain (275×275×36), 24h window (2009-09-01 00Z–2009-09-02 00Z), `irrtm=1`.

### File List

- `Main/radlib/mod_rrtmg_driver.F90` (modified)
- `Main/radlib/rrtmg_lw_rad.F90` (modified)
- `Main/radlib/rrtmg_sw_rad.F90` (modified)
- `Main/radlib/mcica_random_numbers.F90` (modified — Task 1b, gfortran portability fix, franco-requested)
- `configure.ac` (modified — `COMPILER_PGI` detection fix, franco-requested)
- `bin/slurm-build-gnu-debug.sh` (new, **not tracked** — see note below)
- `bin/slurm-build-gnu-production.sh` (new, not tracked)
- `bin/slurm-build-intel-debug.sh` (new, not tracked)
- `bin/slurm-build-intel-production.sh` (new, not tracked)
- `bin/slurm-build-nvhpc-debug.sh` (new, not tracked)
- `bin/slurm-build-nvhpc-production.sh` (new, not tracked)
- `bin/slurm-task2b-build-and-run.sh` (new, not tracked — Task 2b full-executable build + run)
- `bin/slurm-task2b-run.sh` (new, not tracked — Task 2b run-only, reuses a prior build)
- `bin/slurm-task3-profile.sh` (new, not tracked — Task 3 profile-build + perf/nsys run)
- `bin/task3-rank0-perf-wrap.sh` (new, not tracked — Task 3 rank-0-only perf wrapper)
- `bin/task3-rank0-nsys-wrap.sh` (new, not tracked — Task 3 rank-0-only nsys wrapper)

**Note (code review, 2026-07-07):** the 11 `bin/*.sh` scripts above are intentionally **not** committed to git — `bin/.gitignore` was never actually modified (it remains just `*` / `!.gitignore`) and all 11 scripts show as ignored/untracked in `git status`. Per franco's explicit call during code review, this is deliberate: these are personal HPC verification/profiling tooling, not part of this story's tracked deliverable. They exist on disk at the paths above for reference and reproducibility, but a fresh `git clone` of this branch will not include them.
