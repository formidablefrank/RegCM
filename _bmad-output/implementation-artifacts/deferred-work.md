# Deferred Work

## Deferred from: code review of 1-1-verify-and-commit-the-regression-diff-tool (2026-08-11)

- `Main/mod_lm_interface.F90:930`, `lm_csrate => lm%csrate` — `forrtl: severe (408)`, "A pointer with the CONTIGUOUS attribute is being made to a non-contiguous target," inside `collect_output`/`surface_model`. Blocked this story's Slurm-based runtime verification: both twin `test_001.in`-derived runs (Intel `--enable-debug` build) crashed here immediately after their t=0 initialization dump, so the RAD/SRF/STS output streams never reached their first flush and the story's bit-exact/tolerance evidence rests on the ATM stream's t=0 record alone rather than a full physics-timestep comparison across all streams. This is the same crash site already described in more detail in `spec-4-2-instrument-kpp-chemistry-integration.md`'s Spec Change Log — a second, independent DEBUG build hitting the identical guard. **Owner/next hook (added during `/bmad-code-review`):** Story 1.2 (Verify Multi-Process-Count Comparison) is next in line to run real Slurm/DEBUG-build verification and is the most likely candidate to hit this exact crash a third time — prioritize root-causing this (DEBUG build's `-check` flags vs. a genuine contiguity bug) before that story's runtime verification starts, or budget time in that story to work around it the same way this one did. Not investigated or fixed here (out of this story's verify-and-document scope).
- `Tools/Scripts/TestingAndBenchmarking/regression_diff.py`'s field comparison (`compare_variable`) calls `np.array_equal(c, b)` on each field's full array; on two zero-length arrays (a stream with no time records, e.g. because the run crashed before its first flush) this returns `True` unconditionally, and `judge()` reports it as `"status": "exact"` — a vacuous pass indistinguishable in the tool's own report from a real bit-exact comparison. Surfaced during Story 1.1's own verification (see `1-1-verify-and-commit-the-regression-diff-tool.md`'s Outcome/Caveat): 3 of 4 compared streams were empty due to the `mod_lm_interface.F90:930` crash above, and the tool reported them "bit-exact" alongside the one stream with real data. A hardening fix (e.g. `compare_variable` or `judge()` flags/warns when a compared field has zero records on both sides, rather than reporting silent success) is out of Story 1.1's scope — it verifies the tool's existing, documented behavior, and doesn't redesign it per that story's Boundaries.
- `Tools/Scripts/TestingAndBenchmarking/regression_diff.py`'s top-of-file usage comment (fixed for its stale `compare`-subcommand text in Story 1.1) is a `#`-comment block, not a real Python docstring — `ast.get_docstring()` returns `None`, so `__doc__` is `None` and `main()`'s `argparse.ArgumentParser(description=__doc__)` never surfaces this usage text via `--help`. Converting the header to a real triple-quoted module docstring (preserving its content) would make the corrected invocation example actually reachable by a contributor running `--help`, not only by reading source. Pre-existing structural choice, not introduced by Story 1.1's one-line fix — minor, cosmetic priority.
- `_bmad-output/planning-artifacts/epics.md`'s own Story 1.1 acceptance-criteria text still reads "Given both scripts are currently untracked... Then... are committed" — the exact premise this story found stale during implementation (both were already committed in `238393c72` before the epic began). The correction landed in the derived spec and in `epic-1-context.md`, not in `epics.md` itself; since `epic-1-context.md`'s header says it's meant to be regenerated from planning docs, a future regeneration risks silently reintroducing the false premise. Correcting a Phase 1-3 planning artifact is outside a normal implementation story's scope — flagged here rather than edited directly; revisit via `correct-course` or the next planning touch.
- `Tools/Scripts/TestingAndBenchmarking/regression_diff.py`'s other documented error paths — `shape-mismatch`, `missing-in-candidate`, `missing-in-baseline` (`regression_diff.py:159-162,79-80`), and the "file fails to open" `unreadable` path (`regression_diff.py:50-53`) — were never runtime-exercised by Story 1.1, whose approved I/O Matrix covered only same-run-bit-exact, tolerance-override, and malformed-tolerance-file. Good candidate for Story 1.2 (Verify Multi-Process-Count Comparison) to extend coverage into, since it will also be running the tool against real multi-rank output.
- The GPU tolerance policy's "7-model-day run" validity precondition (stated in `epic-1-context.md`'s Requirements & Constraints, mirroring the PRD) is asserted as policy but never checked, recorded, or referenced anywhere `regression_diff.py` actually runs — the tool judges purely field-by-field with no run-duration awareness, and Story 1.1's own verification exercised only a single t=0 record. No action needed for CPU-only bit-exact comparisons (duration is irrelevant there); relevant once GPU statistical-tolerance comparisons (Epic 10) start being run through this tool.
- Story 1.1's DEBUG-build runtime verification never tried a non-DEBUG (production) build as a way to sidestep the DEBUG-only `mod_lm_interface.F90:930` crash above and obtain genuine (non-vacuous) evidence across all 4 output streams instead of just ATM. Cheap to try (same fixture, same Slurm setup, drop `--enable-debug`) and worth attempting before or alongside a real fix to the crash itself — would give stronger multi-stream evidence for this story's own AC without needing the underlying bug fixed first.

## Deferred from: code review of 4-2-instrument-kpp-chemistry-integration (2026-08-11)

- Shared DEBUG timer registry in `Main/mpplib/mod_service.F90` (`maxnsubs = 100`) has no bounds check in `time_begin` (`mod_service.F90:165-183`): once `n_of_nsubs` exceeds 100, `time_bt(indx)`/`info_serial(indx)` etc. are written out of bounds — an abort under `-fcheck=bounds` (GNU `--enable-debug` builds add this flag) or silent corruption otherwise. A tree-wide grep found ~135 `call time_begin(` sites already present across `Main/` before this story's 2 additions (`cb6_integrate`, `cbmz_integrate`), spanning dynamics, advection, radiation, PBL, multiple alternative cumulus/ocean/land-surface schemes, and chemistry support routines. Not all sites are reachable in a single run (several are mutually-exclusive alternative physics schemes selected by namelist), but land/ocean cumulus scheme selection (`icup_lnd`/`icup_ocn`) can activate more than one scheme at once, and a full-physics production configuration (dynamics + radiation + PBL + cumulus + microphysics + land surface + chemistry) plausibly registers well past 100 distinct labels. This independently substantiates a risk story 1.2's own Task 4 already flagged as unverified at runtime ("Sanity-check `n_of_nsubs` stays under `maxnsubs = 100` cap"). A fix (e.g. `if (n_of_nsubs >= maxnsubs) call fatal(...)` in `time_begin`, mirroring the existing `indx < 0` check already present in `time_end`) is out of scope for story 1.2 — `mod_service.F90` is shared infrastructure used by 40+ files, not owned by this story's two integrator files.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-instrument-kpp-chemistry-integration.md`
  summary: MOLOCH's chemical boundary relaxation (`morelax_chiten`) SIGSEGVs under `idynamic=3` + `ichem=1` + `ichebdy=0`, with no fallback path for the `ichebdy=0` case.
  evidence: `Main/chemlib/mod_che_bdyco.F90:992`/`:1010`. Called unconditionally from `Main/mod_moloch.F90:501` (`if (ichem==1) call morelax_chiten(trac)`) whenever `ichem==1` under MOLOCH, with no `ichebdy` guard, while `chib1` is only allocated `if (ichebdy==1)`. Confirmed toolchain-independent (reproduced identically on two separate Intel builds). Confirmed not a namelist misconfiguration (the `nspgx`/power-of-2 precondition, item below, was independently verified satisfied). The non-MOLOCH `mod_bdycod.F90`/`chem_bdyval` path does have an `ichebdy=0` fallback; MOLOCH's does not. Affects any `idynamic=3` + `ichem=1` + `ichebdy=0` configuration.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-instrument-kpp-chemistry-integration.md`
  summary: `drydep_gas`/`stomtresis` indexes `DRYDEPVG` with subscript 0 (below its lower bound of 1), followed by cascading floating-point divide-by-zero on other ranks.
  evidence: `Main/chemlib/mod_che_drydep.F90:841`/`:862`. Seen once, on an EUR12 MOLOCH fixture with `ichsursrc=0`; not reproduced/isolated further once the investigating story relocated to a different domain. Plausibly a land-use/vegetation-type classification left at 0 for some column when surface emission source data isn't supplied.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-instrument-kpp-chemistry-integration.md`
  summary: `relax_coefficients` `fatal()`s with "nbl np not a power of 2" whenever `nspgx-2` isn't a power of 2 — a real footgun, not itself a bug, but the shipped `Testing/test_008.in` fixture (`nspgx=12`, giving 10) would hit it if ever actually run.
  evidence: `Main/mpplib/mod_runparams.F90:677`. Consistent with the broader finding (this story and others) that several `Testing/*.in` fixtures with `ichem=1` appear never to have been run end-to-end in this codebase's history.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-instrument-kpp-chemistry-integration.md`
  summary: `PreProc/Terrain` hard-restricts `iproj='ROTLLR'` domains to `idynamic=3` (MOLOCH) by design, not a bug — relevant to anyone trying to reuse a MOLOCH domain's geometry under a different dynamical core.
  evidence: `terrain.F90` explicitly refuses to generate a domain with `iproj='ROTLLR'` for any other `idynamic`, with the explicit message "Rotated Longitude/Latitude projection supported only by MOLOCH."

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-instrument-kpp-chemistry-integration.md`
  summary: `collect_output`/`surface_model` crashes with `forrtl: severe (408)` ("A pointer with the CONTIGUOUS attribute is being made to a non-contiguous target") on the `lm_csrate => lm%csrate` pointer assignment, in DEBUG builds — the same crash site already tracked as Epic 1 retro action item 4 (still open) and in this ledger's Story 1.1 entry above.
  evidence: `Main/mod_lm_interface.F90:930`, reached via `mod_tendency.F90`'s `physical_parametr`/`tend`. Not investigated past the crash message by this story; this is the bug franco chose to stop pursuing further rather than keep pushing through, given it already blocked two other stories (1.1, 1.2-verify-multi-process-count-comparison) independently.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-instrument-kpp-chemistry-integration.md`
  summary: `aeroppt`'s `RH` array read uses subscript 19 against a declared upper bound of 18 (`forrtl: severe (408)`), triggered whenever `ichem==1`, independent of `iclimaaer`. Diagnosed but not fixed.
  evidence: `Main/radlib/mod_rad_aerosol.F90:2511`. `aeroppt`'s `rh(n,k)` reads are unconditional, but `rt%rh` (allocated shape `(kz,npr)` in `Main/radlib/mod_rad_colmod3.F90:116`) is only correctly indexed as `rh(k,n)`. The neighboring `pint`/`path` computation already has an `irrtm`-branched transpose fix for this exact pattern (`mod_rad_aerosol.F90` around line 2455); `rh` never received the equivalent treatment. Still open.

## Deferred from: code review of 4-1-instrument-rrtmgs-compute-core (2026-07-07)

- Heavy duplication across the 11 new `bin/` scripts (module-load blocks and `irrtm`-forcing namelist logic repeated near-verbatim 5+ times). A shared helper script (e.g. `bin/lib/task-common.sh`, sourced by each job script) would remove the duplication next time one of these scripts needs a change.
- All new `bin/` scripts hardcode franco's personal scratch paths (`/leonardo_scratch/fast/ICT26_MHPC_0/franco/...`) with no parameterization, limiting reuse by other team members or in a future CI context.
- Ephemeral Slurm job-ID references baked into permanent script comments (e.g. "job 48758729" in `bin/slurm-task3-profile.sh`) are unretrievable to a future reader outside this account/timeframe — worth rephrasing as a general rule ("MPI ranks on this cluster run as independent Slurm job steps...") without the specific job-ID citation next time these comments are touched.
- `Share/mod_heatindex.F90`'s DEBUG+OpenACC GPU-port fix (`#if defined(DEBUG) && !defined(OPENACC)` guarding the DEBUG-only diagnostic that calls an un-annotated `fatal()` from inside `!$acc routine seq` code) was applied and verified twice during Story 1.1, then intentionally removed by franco and deferred to the GPU-offloading epic. See Story 1.1's Dev Agent Record for the full root-cause writeup and verification evidence (job 48709086 reverted/CPU-only, job 48720240 fix reapplied/real GPU codegen confirmed via `-Minfo=accel`). **Status update (2026-08-21, story 4-3, `instrumentation/diagnostic-io-4-3` branch):** franco directed re-applying this fix again (all 31 occurrences, same transformation, confirmed identical starting count) after Story 4.3's NVHPC build scripts were changed to add `--enable-openacc-stdpar` and hit the identical `NVFORTRAN-S-1061` failure — Story 4.3 itself has no GPU need; the flag was added for other reasons. The fix is therefore currently present in the tree on this branch, not absent — check current branch state before assuming this entry describes the tree as it stands.
- `Main/clmlib/clm4.5/mod_clm_snicar.F90` fails to compile under `--enable-clm45 --enable-openacc-stdpar` on NVHPC 26.3 — confirmed (jobs 53397830, 53399162), and a **different** error class than the `mod_heatindex.F90` entry above: `NVFORTRAN-S-1058-Call to NVHPC runtime function not supported - pgf90io_begin`, not `NVFORTRAN-S-1061`. **Four** blocks of unconditional `write(stderr,*)` diagnostic statements inside `SNICAR_RT`'s `!$acc loop seq` regions, found one at a time as each fix let the build progress to the next: (1) a snow-grain-radius bounds check, (2) an infinite-loop/"wormhole" guard, (3) an energy-conservation check, (4) an albedo-greater-than-1.0 check. NVHPC's `-stdpar=gpu` cannot lower Fortran I/O to device code at all, a stricter restriction than the un-annotated-procedure-call class `mod_heatindex.F90` hit. All four blocks already had partial `OPENACC` awareness — a mistaken `#ifdef OPENACC`-branched `write` that dropped `trim()` (assuming that alone was the GPU-incompatibility, when any `write` at all is disallowed), plus an existing `#ifndef OPENACC call fatal(...) #else found = .true. #endif` split around the terminal action — but the file had never actually been build-verified under `-stdpar=gpu` before, so this gap was latent. **Status update (2026-08-21, story 4-3, franco-directed, `instrumentation/diagnostic-io-4-3` branch):** fixed by wrapping each full diagnostic-write block (not just the terminal `fatal()` call) under `#ifndef OPENACC`/`#else found = .true.`, removing the mistaken `trim()`-only OPENACC branches entirely. No prior verified precedent for this exact fix, freshly authored this session, not carried over from an earlier story — confirmed no further `write(stderr` call sites remain unguarded anywhere in this file (grep-verified across all four subroutines). Same caveat as the `mod_heatindex.F90` entry: this class of fix belongs to the GPU-offloading epic's territory, applied here only because Story 4.3's build tooling needed `--enable-openacc-stdpar` to succeed for unrelated reasons.
- After the `mod_heatindex.F90`/`mod_clm_snicar.F90` fixes above, `--enable-openacc-stdpar` binaries (jobs 53399995, 53402921) compiled cleanly but crashed instantly at runtime on every leg: `Accelerator Fatal Error: The application was compiled with -gpu=unified, but this platform does not support Unified Memory.` A GPU-environment/hardware-compatibility issue tied to `-stdpar=gpu`'s `mem:unified` model on this cluster's compute nodes (`lrdn2995`), not a code bug — not investigated further (out of scope). `--enable-openacc-debug` (autocompare) was tried next and failed at `configure` time instead: `nvfortran-Error-The -acc=gpu suboptions managed and autocompare are not compatible with each other` / `Redundant and Autocompare are not supported with Unified or Managed memory modes` (`config.log`, confirmed via the actual failed `conftest.f` compile, not just the high-level "NetCDF module not found" summary) — `-acc=gpu` apparently defaults to a managed/unified memory mode on this NVHPC version unless overridden, which autocompare rejects outright. Not resolved; would need the correct explicit `-gpu=` suboption for autocompare mode, unknown at this time.
- `Main/mod_bdycod.F90:4081` fails to compile under `--enable-openacc-managed` (`--enable-clm45`, NVHPC 26.3, job 53438436) with an NVHPC device-compiler internal error: `nvnvvmd: error: parse constant expression type mismatch` / `NVFORTRAN-F-0155-Compiler failed to translate accelerator region (mod_bdycod.F90: 4081)`. A different, more opaque class of failure than the `mod_heatindex.F90`/`mod_clm_snicar.F90` cases above (not a missing-annotation or I/O-in-device-code pattern that can be pattern-matched and fixed the same way) — not investigated past the compiler diagnostic, not fixed. Fourth distinct pre-existing GPU-porting bug surfaced this session, in a fourth different file, across three different `--enable-openacc-*` variants (`stdpar`, `debug`, `managed`) each hitting something different. **Status update (2026-08-21, story 4-3, franco-directed):** after this fourth wall, franco directed abandoning the GPU-flag chase entirely and falling back to plain `--enable-debug` (no GPU flags) for Story 4.3's build scripts, since the story only needs CPU-side I/O-timer verification. The pattern across all four bugs (one per file, one per variant, none overlapping) suggests this codebase's GPU-annotation coverage is genuinely incomplete outside the handful of routines already verified for GPU (`getcape_new`, `mod_clm_hydrology2.F90`, `mod_interp.F90`, `mod_heatindex.F90` after this session's fix) — a real signal for whoever picks up the GPU-offloading epic (Epic 10): expect more files like `mod_bdycod.F90` and the autocompare/managed-mode incompatibility to surface once broader GPU-flag builds are attempted, not just the routines this project has profiled so far.
- **Follow-up (2026-08-22, story 4-3, franco-directed): `mod_bdycod.F90`'s `nvvmCompileProgram` ICE investigated much further and conclusively characterized as systemic, not a source defect.** Franco directed a second, extended investigation into this bug (continuing from the entry above). Nine distinct mitigation attempts were tried against `motopnudge`'s four `do concurrent` kernels (`w`/`t`/`u`/`v` loops), each producing the *identical* `parse constant expression type mismatch` error, just relocated: (1) copying loop bounds into local scalars before the `v` loop — failed, error persisted on the same loop; (2) escalating the `v` loop to explicit `!$acc parallel loop collapse(3) gang vector`, matching this file's own proven working pattern at ~line 1372 — failed identically; (3) making only the `v` loop plain sequential Fortran (no device codegen at all) — the error then moved to the `u` loop, which had been untouched, proving the failure tracks *whichever kernel is last*, not `v`'s specific content; (4) making all four `motopnudge` loops sequential — got past `motopnudge` entirely, but the error then moved to `setup_bdywt` (line 4039), the routine immediately before it; (5) franco added Fortran 2018 `shared()` locality-spec clauses to each `do concurrent` (plus fixed `t,u,v,w`'s `intent(in)` to `intent(inout)`, a genuine, unrelated correctness fix since those arrays are mutated) — first attempt hit a real, valid `NVFORTRAN-S-1044-Invalid DO CONCURRENT locality spec variable` error (parameters `trtau`/`wrtau` can't appear in `shared()`), fixed by removing them from the clause; with that fixed, the underlying ICE returned identically on the `v` loop; (6) dropping `-stdpar=gpu` from the `--enable-openacc-managed` configure.ac branch (so `do concurrent` no longer attempts device lowering at all) — the failure moved to a completely different construct type, `!$acc routine seq` in subroutine `tau` (line 3751), proving the defect is not specific to `do concurrent` and also hits core explicit-OpenACC constructs this project's own documented GPU pattern depends on (`getcape_new`, `mod_heatindex.F90` use the same `!$acc routine seq` construct); (7) dropping `lineinfo` from the `-gpu=` suboptions (testing whether debug-metadata accumulation across the file's 25+ device regions was the resource being exhausted) — no change, identical error; (8) pinning `PGI_GPU_ARCH` to `cc80` (Leonardo's actual A100 compute capability) instead of `ccnative` — no change, byte-for-byte identical error and line; (9) reverting `mod_bdycod.F90` to its fully original state and dropping `-stdpar=gpu` from the managed variant — the failure returned to `tau`'s `!$acc routine seq`, confirming (6). **Conclusion:** this is a genuine NVHPC 26.3 backend defect at the compilation-unit scale (`mod_bdycod.F90` has 25+ `do concurrent`/`!$acc` device regions) — the ICE always hits whatever GPU construct is last-standing once earlier ones are removed or disabled, regardless of construct type (`do concurrent`, `!$acc parallel loop`, `!$acc routine seq`) or compiler flags (`-stdpar=gpu`, `lineinfo`, `PGI_GPU_ARCH` all ruled out as causes). Not fixable by editing loops or flags one at a time. **The only remaining untried lever is splitting `mod_bdycod.F90` into smaller compilation units** so fewer device kernels get compiled together per `nvfortran` invocation — a real structural refactor (module boundaries, `use` statements, build wiring), scoped but not attempted this session. Franco directed stopping here and reverting Story 4.3's build scripts to plain `--enable-debug` (no GPU flags), since the story's own verification need is CPU-only. `mod_bdycod.F90` itself was reverted to its original, pre-session state (no GPU-related edits kept) — the `shared()`/`intent(inout)` correctness fix was also reverted along with it, since it was entangled with the abandoned experiment; worth re-applying on its own merits (real correctness fix, independent of any GPU work) whenever this file is next touched.

- **General insight for future DEBUG-timer instrumentation (not itself a bug, a project-wide gotcha):** `Main/mpplib/mod_service.F90`'s `time_print` (`mod_service.F90:259-271`) requires every MPI rank to register the exact same *count* of distinct DEBUG timer labels (`n_of_nsubs`) by the end of the run — enforced via `allgather_i` across ranks, fatal-erroring `"different trees on different pe!"` on any mismatch. Story 4.3 hit this directly: `Main/mpplib/mod_ncout.F90`'s new `io_gather_hist`/`io_write_hist`/`io_wait_hist` timers were initially added only to the code paths that the I/O rank (`myid == iocpu`) executes, leaving non-I/O ranks' early-return branches (serial/`.not. parallel_out` mode) with fewer registered labels — a crash caught only via genuine multi-rank serial-mode execution (2026-08-22, job 53444070), never by static review or single-rank/parallel-mode testing. Fixed in that file (see spec-4-3's Spec Change Log) by registering every label on every rank-conditional branch, using a truthful zero-elapsed-time `time_begin`/`time_end` pair where a rank genuinely does no work in that phase. **Anyone adding DEBUG timer instrumentation to a code path with `myid`-conditional branches (early returns, io-rank-only sections, etc.) should register the same label set on every branch, and verify with an actual multi-rank run — not just a compile check — before considering the instrumentation done.**
- The 11 `bin/*.sh` HPC verification/profiling scripts from Story 1.1 (`slurm-build-*.sh`, `slurm-task2b-*.sh`, `slurm-task3-profile.sh`, `task3-rank0-*-wrap.sh`) are intentionally untracked/gitignored personal tooling, per franco's explicit call during code review — not a gap to close, just noting the deliberate choice so a future reviewer doesn't rediscover and re-litigate it. If this tooling should later become team-shared (e.g. for Epic 1's remaining stories, which reuse the same build/profiling pattern), revisit adding a real `bin/.gitignore` exception and parameterizing the hardcoded personal paths at that point.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-instrument-kpp-chemistry-integration.md`
  summary: 3 of the 7 real IFS driving-data files under `RegCM-data/EURR12/RCMDATA/IFS/` are truncated/incomplete downloads, surfaced while constructing a small-domain fixture for Story 4.2's runtime-verification attempt.
  evidence: `IFS_2018102618+000.nc` at 268MB, `IFS_2018102706+000.nc` at 844MB, `IFS_2018102712+000.nc` at 663MB, vs. ~1004MB for the 4 complete files. Worth re-fetching if that data is needed again by a future story.

## Deferred from: code review of 1-2-verify-multi-process-count-comparison (2026-08-19)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-verify-multi-process-count-comparison.md`
  summary: AD-12's mandated `nproc=1`/`nproc=4` minimum pair is genuinely verified for the ATM stream only (one bit-exact t=0 record); RAD/SRF/STS are unverified at all four `test_001.in`-driven counts (1/4/16/64), not just the pair.
  evidence: `test_001.in`'s `ipptls=1` reliably triggers the `mod_lm_interface.F90:930` crash immediately after ATM's t=0 flush (confirmed via real stderr in this story: `forrtl: severe (408)` in `collect_output`/`surface_model`), so RAD/SRF/STS never reach their first flush and stay zero-length on both RUN and BASELINE. `regression_diff.py`'s `compare_variable` reports two empty arrays as vacuously "exact" (the same gap Story 1.1 already flagged, `np.array_equal` on zero-length arrays), so `--nprocs`'s `pass: true` headline doesn't distinguish this from genuine multi-stream verification. Closing this needs either the non-DEBUG-build workaround already deferred from Story 1.1 (untried), or a low-`nproc`-feasible fixture with `ipptls` set to avoid the crash guard (mirroring why EUR12's `ipptls=2` avoided it at `nproc=196`), or a zero-record guard added to `regression_diff.py` itself (out of scope for a "verify existing tool" story).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-verify-multi-process-count-comparison.md`
  summary: `bin/slurm-1-2-submit-queue.sh`'s resumable driver has three latent robustness gaps that didn't manifest this run (every leg succeeded) but would corrupt bookkeeping or violate the spec's own "HALT per Ask First" contract on a future failure.
  evidence: (1) `has_output=$(grep -c PATTERN "$outfile" 2>/dev/null || echo 0)` — `grep -c` exits 1 on zero matches despite printing "0", so the `||` fallback appends a second "0" line, breaking the later `[ -ge 1 ]` integer test on a genuine failure. (2) `wait_for_slot()` blocks on any queued job for the user (`squeue -u "$USER" -h`), not scoped to `dcgp_qos_dbg` specifically, so an unrelated job elsewhere would stall the driver. (3) On `NOT_DONE`/`SUBMIT_FAIL` the driver logs and moves to the next queue item rather than halting, even though the spec's I/O & Edge-Case Matrix requires "HALT per Ask First" on a fixture failing/timing out at its assigned count — only the expected `test_001.in` crash path is actually handled (via `OUTPUT_PRESENT` detection), not a genuine unexpected failure.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-verify-multi-process-count-comparison.md`
  summary: The EUR12 `nproc=196` leg's ~27-minute actual runtime against `dcgp_qos_dbg`'s 30-minute hard cap is a tight, unremarked margin for future reruns of this exact leg.
  evidence: Jobs 52648082/52648968 both completed in ~27 min, roughly 90% of the QOS ceiling, with no `--exclusive` flag guarding against node-sharing jitter that could push a rerun over the limit and force a resubmission under `normal` QOS (the same escape hatch this story already needed for the `nproc=1` EUR12 probe before the fixture split).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-verify-multi-process-count-comparison.md`
  summary: `regression_diff.py`'s JSON report carries no per-`nproc-<N>`-block metadata identifying which fixture/domain produced it, so a future reader of just `report_nprocs_*.json` (without this story's spec) can't tell that the `nproc=196` block is a structurally different domain (EUR12, 275×275) from `nproc=1/4/16/64`'s (`test_001.in`, 34×64).
  evidence: Confirmed directly in this story's own `report_nprocs_1-4-16-64-196.json` — each `nproc-<N>` key's `files` dict lists only field-level comparison results, no fixture/domain/namelist identifier anywhere in the structure. Tool enhancement; out of this story's scope (Boundaries: don't modify `regression_diff.py` beyond what verifying existing behavior requires).

## Deferred from: code review of 1-3-retire-the-legacy-regression-scripts (2026-08-19)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-retire-the-legacy-regression-scripts.md`
  summary: `Tools/Scripts/archive/` has no `README`/index recording why `testing.py` and `preproc-compare.py` were archived or pointing to `regression_diff.py`/`manage_baseline.py` as their replacement.
  evidence: The story's own purpose is "no one mistakes either leftover script for a working, general-purpose tool" — a bare directory of two old scripts doesn't itself communicate that on inspection; only the commit message (not discoverable by someone just browsing the tree) records the reason. Adding the index wasn't part of this story's named scope (move + commit message), so it's deferred rather than added unrequested.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-retire-the-legacy-regression-scripts.md`
  summary: `Tools/Scripts/BuildBot/` still holds `testing.py`'s now-orphaned support files (`parsing_editing.py`, `nc_stuff.py`, `argo.cfg`, `example.cfg`) plus the same-vintage, equally non-runnable Python 2 `configure-auto` script, none of which this story's AC named.
  evidence: `Tools/Scripts/archive/testing.py:26` does `import parsing_editing,nc_stuff` — those two modules, and the two `.cfg` files written for `parsing_editing.parse_config()`'s exact key set, stayed behind in `BuildBot/` unmoved. `configure-auto` shares `testing.py`'s author, era, and dead `print "..."`/`#!/usr/bin/python` syntax but wasn't named in Story 1.3's scope (epics.md only names `testing.py` and `preproc-compare.py`), so expanding the move to cover it wasn't authorized here. Revisit as a follow-up `BuildBot/` cleanup if a future story wants that directory fully retired rather than partially.

## Deferred from: code review of 1-4-establish-trusted-baselines-for-testing-fixtures (2026-08-19)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-establish-trusted-baselines-for-testing-fixtures.md`
  summary: No OASIS3-MCT/REGESM-coupled-build baseline fixture exists anywhere in the project (`Testing/` or `RegCM-data`), so FR-27's "coupling builds" baseline target (FR-13/FR-14) is still unsatisfied after this story — only the EUR12 (idynamic=3 MOLOCH, uncoupled) baseline was promoted.
  evidence: Searched `Testing/*.in`, `Testing/CORDEX/*.namelist`, and `RegCM-data` for any namelist enabling coupling: no file sets `ioasiscpl = 1` or carries an `&oasisparam` block, the only namelist-level gate that actually requires the `--enable-oasis`/`-DOASIS` build (`Main/mod_params.F90:1148-1169`). `Testing/test_016.in` sets `iocncpl = 1` and reads a generic `&cplparam` block, but that flag alone drives internal ocean/wave-coupling array allocation (e.g. `Main/mod_atm_interface.F90:938`) independent of OASIS, and the file does not enable `ioasiscpl` — so it is not an OASIS3-MCT/REGESM-coupled fixture. Correction from this story's code review: REGESM's code path is not absent from the source tree — `Main/mod_update.F90` (`RCM_Get`/`RCM_Put`/`RCM_Allocate` over `imp_data`/`exp_data`, gated by `icopcpl`, defaulting to 0 in `Main/mod_params.F90:304`) is REGESM's field-exchange mechanism, confirmed by the PRD itself (`_bmad-output/planning-artifacts/prds/prd-RegCM-2026-07-04/prd.md:59`: "REGESM — A second, independent RegCM5 coupling mechanism (`--enable-cpl`, `mod_update.F90`)"; FR-14, same file line 268, names the same module/fields). No `Testing/*.in`/`RegCM-data` namelist sets `icopcpl = 1`, so the mechanism exists but is unexercised by any fixture and has no field-list documentation or regression run — that is the real gap. Owned by Epic 13 (`13-1-oasis3-mct-field-exchange-documentation-and-regression-coverage`, `13-2-regesm-field-exchange-documentation-and-regression-coverage`), not yet started (`sprint-status.yaml`); this story's Boundaries explicitly forbid fabricating a coupled namelist to fill the gap.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-establish-trusted-baselines-for-testing-fixtures.md`
  summary: The AD-8 canonical EUR12 fixture (now promoted as the `$FAST` trusted baseline, and copied verbatim into `Testing/EUR12_namelist.in`) declares `ifshf = .true.`, `iflak = .true.`, `ifsub = .true.`, and `ifchem = .true.` in `&outparam`, but all four are silently forced back to `.false.` at runtime.
  evidence: The primary, unconditional cause: the same block sets `ifcordex = .true.`, and `Main/mod_params.F90:1182-1192` forces `ifshf`, `iflak`, `ifsub`, `ifopt`, and `ifchem` all to `.false.` whenever `ifcordex` is true, regardless of any other namelist setting. Independently and redundantly, `Main/mod_params.F90:1438-1447` also forces `iflak = .false.` when `lakemod /= 1`, `ifsub = .false.` when `nsg < 2`, and `ifchem = .false.` when `ichem /= 1` — and the fixture never sets `lakemod`/`ichem`/`nsg` either (defaults: `lakemod = 0` at `mod_params.F90:306`, `ichem = 0` at `mod_params.F90:307`, `nsg = 1` at `Share/mod_dynparam.F90:565`). Neither `Testing/EUR12_namelist.in` nor the `$FAST` canonical copy it was reproduced from sets any of these, so this is a pre-existing property of the canonical fixture itself, not something this story introduced — surfaced incidentally by code review, out of scope to fix here since this story's mandate was a faithful reference copy (AD-8), not fixture redesign.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-establish-trusted-baselines-for-testing-fixtures.md`
  summary: The promoted EUR12 baseline is the AD-8 canonical fixture, but as configured (`do_parallel_netcdf_out = .false.`) it exercises only the serial gather-write output path (FR-6), not the parallel-write path (FR-5) — so this story's promoted baseline is not actually representative of FR-5/FR-6's parallel-I/O concern despite epics.md's Story 1.4 AC1 naming FR-5/FR-6 as one of the three targets this baseline should cover.
  evidence: `Testing/EUR12_namelist.in`'s (and the `$FAST` canonical copy's) `&outparam` sets `do_parallel_netcdf_in = .true.` but `do_parallel_netcdf_out = .false.`. This is inherited verbatim from the canonical fixture, not introduced by this story, and out of scope to change here (AD-8 mandates a faithful reference copy, not fixture redesign) — but a future story enabling/validating parallel diagnostic write (FR-5, Epic 11) should not assume this baseline already covers that path.

## Deferred from: code review of 4-3-instrument-diagnostic-output-gather-and-write (2026-08-21)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-instrument-diagnostic-output-gather-and-write.md`
  summary: `Main/mod_savefile.F90` fails to compile under NVHPC 26.3 with `--enable-pnetcdf` — `NVFORTRAN-S-0166 mpi_fortran_<sym> cannot be a common block and a subprogram`, for `weights_empty`/`unweighted`/`statuses_ignore`/`status_ignore`/`errcodes_ignore`/`argvs_null`/`argv_null`/`in_place`/`bottom`, repeated across all four of the file's subroutines (`savecreate`/`saveready`/`savedefdim`/`saveclose`). Blocks any full RegCM5 build configured `--enable-pnetcdf` on this toolchain, independent of this story's own changes. **Root cause investigated (2026-08-22, franco-directed):** the compiled `pnetcdf.mod` itself does `use mpi private` internally (confirmed via `strings` on the `.mod` file — `pnetcdf.mod` was built against the classic F90 `mpi` module, not `mpi_f08`), which collides with this file's `use mpi_f08` when both are in scope. **First fix attempt (disproven):** narrowed the module-level `use mpi_f08, only: mpi_comm, mpi_comm_self` (line 34, visible file-wide) down to local `use` statements inside only the two subroutines (`saveopen`, `savecreate`) that actually reference `mpi_comm`/`mpi_comm_self` — rebuilt (job 53622275), **identical failure persisted unchanged**, including in `saveready`/`saveclose`/`savedefvar`, which have zero direct `mpi_f08` references at all. This disproves the "module-level scope" hypothesis: the conflict isn't coming from `mod_savefile.F90`'s own `use mpi_f08` statements — it must be leaking in transitively, most likely via `use mod_mppparam` (module-level, unrestricted `use`, and `mod_mppparam.F90` almost certainly uses `mpi_f08` internally itself, e.g. for `get_cartcomm`'s `type(mpi_comm)` return type). The reverted fix attempt is not kept (no benefit, added diff for nothing). **A real fix would need to touch `mod_mppparam.F90`'s own `mpi_f08` usage** — a core, widely-shared module used throughout the codebase, well outside a scoped fix to `mod_savefile.F90` alone; not attempted. **Second fix attempt (2026-08-22, franco-directed, vendor-documented workaround, root cause now fully confirmed but impractical):** franco found NVIDIA's own HPC SDK 26.3 release notes stating this exact limitation: *"When using Open MPI with nvfortran, mixing `use mpi` and `use mpi_f08` in the same application is not supported. A potential workaround is to add compiler option `-Msecond_underscore` [and] perform a clean rebuild of the application and libraries."* This independently confirms the root-cause diagnosis above — it's a genuine, documented NVHPC/Open MPI limitation, not a RegCM source bug. Applied `FCFLAGS=-Msecond_underscore` to the pnetcdf build (configure.ac's FCFLAGS-appending pattern preserves command-line-supplied flags through the whole chain, verified in the actual compile command) and did a clean rebuild (job 53623089). **Result: the original `NVFORTRAN-S-0166` `mod_savefile.F90` error is completely gone** — confirms the fix genuinely addresses the root cause — **but a new, worse problem replaces it**: `-Msecond_underscore` changes Fortran external-symbol name-mangling globally (not just for the conflicting MPI sentinel symbols), so linking now fails against every pre-built Fortran library in the dependency stack, all of which were compiled with the default (single-underscore) convention: `undefined reference to netcdf_nf90_open__`, `pnetcdf_nf90mpi_open__`, even `mpi_init_f08__`/`mpi_abort_f08__`/`mpi_finalize_f08__` (MPI itself). This is exactly what NVIDIA's own caveat warned about ("clean rebuild of the application **and libraries**") — the pre-built `netcdf-fortran-4.6.1`, `parallel-netcdf-1.12.3`, and HPC-X MPI Fortran libraries (all spack-installed) would need rebuilding with the matching flag too. That's an HPC-admin-level operation (rebuilding shared spack packages used cluster-wide), not something achievable from within this repository. Reverted the `FCFLAGS=-Msecond_underscore` build-script change. **Status: root cause conclusively confirmed (vendor-documented Open MPI + nvfortran `mpi`/`mpi_f08` mixing limitation), workaround identified but requires a library-stack rebuild outside this project's scope — the actionable fix, if ever pursued, is coordinating with CINECA/cluster admins to provide `-Msecond_underscore`-built variants of netcdf-fortran/parallel-netcdf/HPC-X, or restructuring RegCM's own `mpi_f08` usage (`mod_mppparam.F90` and downstream) to avoid `mpi_f08` entirely in any file that also touches PnetCDF.**
  evidence: Job 53331537 (`bin/slurm-4-3-fullbuild-nvhpc-pnetcdf-debug.sh`), full build log at `RegCM-data/build-logs/nvhpc-fullbuild-pnetcdf-debug-4-3-53331537.{out,err}`. Configure succeeded (`CONFIGURE_EXIT=0`); `make` died mid-build at `mod_savefile.o`. Confirmed unrelated to this story's instrumentation: `Main/mpplib/mod_ncout.F90` (the file this story modified) compiled and archived cleanly earlier in the same build log, well before the failure. Root cause not investigated past the compiler diagnostic — `mod_savefile.F90:34` does `use mpi_f08, only : mpi_comm, mpi_comm_self` while `mod_savefile.F90:596` does `use pnetcdf` inside a `PNETCDF`-guarded subroutine; the two libraries' bundled legacy-MPI Fortran interfaces appear to collide under NVHPC 26.3. Blocked this story's planned two-build runtime verification of the PnetCDF-parallel diagnostic-output path (AC #2 of spec-4-3); no `bin/regcm-4-3-debug-nvhpc-pnetcdf` binary could be produced. Fixing `mod_savefile.F90` is out of this story's scope (restart-file I/O, explicitly Story 11.2's territory) — a real fix needs either a `mod_savefile.F90` MPI-interop change or an NVHPC/PnetCDF version change, both out of scope here.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-instrument-diagnostic-output-gather-and-write.md`
  summary: **General NVHPC 26.3 `-Mbounds` gotcha, fixed at two sites, likely more lurking.** `-Mbounds` produces a false-positive "Subscript out of range... upper bound=0" on a *genuinely valid* zero-extent array whenever it appears as (a) an explicit-shape actual argument at a call site, or (b) the source of a `b(lbound(a,1):ubound(a,1)) => a` pointer-bounds remap — even though the array's own declared bounds (e.g. `1:0`) are perfectly legal Fortran. Every non-DEBUG/non-`-Mbounds` build tolerates this fine; only this project's `--enable-debug` NVHPC config (which adds `-Mbounds`) trips it. **Site 1 (fixed):** `Main/cumlib/mod_cu_tiedtke.F90`'s `pxtm1`/`pxtte` getmem calls used `ntr` (real tracer count, 0 with no chemistry) directly as the 3rd-dimension extent; `cucall`'s explicit-shape `pxtm1(kbdim,klev,ktrac)` dummy then crashed at the call site. This blocks the plain `ntr=0` (no-chemistry) baseline too, not just chemistry configs — confirmed via `RegCM-data/story4-3/logs/run-53614700.err:237` (pre-existing serial-leg run, no chemistry, crashed here). Fixed by padding the getmem call to `max(ntr,1)` while leaving the real `ntr` (still possibly 0) as the actual `ktrac` argument passed to `cucall` — output-neutral for any `ntr`, tracer loops inside `cucall` still correctly iterate zero times when `ntr=0`. **Site 2 (fixed):** `Share/mod_memutil.F90`'s `assignp1d_i` (one of ~65 structurally-identical `assignp<rank>d_<type>` pointer-remap helpers, used 670 times codebase-wide) crashed the same way when remapping `Main/chemlib/mod_che_common.F90`'s `idust` (getmem'd to `nbin`, which is 0 for every non-dust `chemsimtype` including CBMZ) via the unconditional `call assignpnt(idust,lm%idust)` in `Main/mod_lm_interface.F90:390` (CLM45-gated, fires whenever `ichem==1` regardless of dust bins). Fixed with a `size(a)==0` special case in `assignp1d_i` only (`b => a` instead of the explicit remap — bounds-identical, avoids the syntax that trips the checker) since that was the only site actually exercised; the other ~64 sibling procedures were deliberately left untouched (no evidence any of them are hit — `chtrsize`, the other `nbin`-sized array, is never `assignpnt`'d anywhere) rather than speculatively patching all of them. **If a future DEBUG-build session hits a similar "Subscript out of range... upper bound=0" crash inside `mod_memutil.F90`'s `assignp*` family, apply the same `size(a)==0` guard to that specific procedure** rather than re-deriving this from scratch.
  evidence: Site 1: `mod_cu_tiedtke.F90:158-167` (comment + fix in place), confirmed via job 53743960 — plain `ntr=0` baseline ran cleanly through 3 real simulated hours (2009-09-01 00:00→03:00 UTC) with no `pxtm1` crash, `RegCM-data/story4-3/logs/test-serial-53743960.out`. Site 2: `mod_memutil.F90:2104-2122`, confirmed via job 53635891 — CBMZ (`ntr=37`, `nbin=0`) run progressed past land-model interface init with no `assignp1d_i` crash after the fix (rebuild job 53635125, PASS).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-instrument-diagnostic-output-gather-and-write.md`
  summary: The HDF5-parallel diagnostic-write dispatch branch (`do_parallel_netcdf_out=.true.` under a plain `--enable-debug` build, no `--enable-pnetcdf`) has never actually completed a runtime exercise in this story. It was attempted once, mid-story, as part of the original planned 3-leg combined run (`RegCM-data/story4-3/slurm_run_story4-3_nvhpc_debug.sh`) and hung indefinitely on its first real exercise of that write path (franco-directed: "do not run parallel hdf5 anymore, run parallel netcdf instead" — which itself turned out blocked, see the separate `mod_savefile.F90`/PnetCDF entry above). This story's final runtime evidence (see the `-Mbounds` entry above) covers the serial path only; PnetCDF-parallel was downgraded to code-inspection-only. HDF5-parallel remains the one of the three diagnostic-output paths (AC #1) with neither runtime evidence nor a code-inspection substitute. Whoever picks this up next should either debug why the HDF5-parallel branch hangs (a distinct issue from anything fixed in this story — none of the `-Mbounds`/`inh3` fixes touch `Share/mod_ncstream.F90`'s HDF5 branch) or explicitly accept code-inspection parity with the PnetCDF-parallel precedent already set in this story's Tasks list.
  evidence: `EUR12_story4-3_hdf5parallel.in` fixture exists, unused since the hang; no successful HDF5-parallel run log exists anywhere under `RegCM-data/story4-3/logs/`.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-instrument-diagnostic-output-gather-and-write.md`
  summary: `Main/chemlib/mod_che_drydep.F90:862` unconditionally used the module-level tracer-index variable `inh3` as an array subscript (`drydepvg(jci1:jci2,inh3) = vdg(9,jci1:jci2)`) without checking it was ever set — `inh3` stays 0 unless a tracer literally named `'NH3'` exists in `chtrname` (`Main/chemlib/mod_che_start.F90:317`), which CBMZ's species list never includes (explicitly documented at `mod_che_common.F90:378`: "does not include any aerosol(NH3)"). Every other `inh3` use site in the codebase (`mod_che_ncio.F90:654,895`, `mod_che_wetdep.F90:512`, `mod_che_isorropia.F90`) already guards this with `if (inh3 /= 0)`/`if (inh3 > 0)` — this was the one inconsistent spot, a genuine pre-existing bug (silent out-of-bounds write on a non-`-Mbounds` build) surfaced only because this story's runtime verification was the first time CBMZ chemistry was actually run under `-Mbounds` on this toolchain. Fixed by adding the same `if (inh3 > 0)` guard used everywhere else. Out of this story's own scope (chemistry drydep, not diagnostic I/O) but low-risk, self-contained, and left in place rather than reverted.
  evidence: `mod_che_drydep.F90:858-866` (guard + comment in place). Crash reproduced pre-fix at job 53635335 (`Subscript out of range for array drydepvg (mod_che_drydep.F90: 862), subscript=0, lower bound=1, upper bound=37, dimension=2`); rebuild (job 53635776) and rerun (job 53635891) progressed past this point with no recurrence.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-instrument-diagnostic-output-gather-and-write.md`
  summary: **Timer-label collision across call sites, structural not a bug — surfaces only under `nsg>1` (subgrid/slab-ocean), never exercised this story.** `Main/mpplib/mod_ncout.F90`'s `write_record_output_stream` and its `writevar2d_output_stream`/`writevar3d_output_stream` siblings each declare their own independent `save`-indexed `time_begin`/`time_end` timers, but all register under the identical mandated label strings `io_gather_hist`/`io_write_hist` (the frozen Boundaries text in spec-4-3 requires these exact labels at every call site — see AD-17). `Main/mpplib/mod_service.F90`'s registry (`time_begin`'s `n_of_nsubs`/`indx`, `time_print`'s per-`nsubs` row loop) keys rows by call-site index, not by label string, so any run that exercises both `write_record_output_stream` (main streams) and `write_vars_slaboc_stream`→`writevar2d_output_stream` (slab-ocean stream, reachable only when `nsg>1`) in the same process will print two or three separate, indistinguishable rows all labeled `io_gather_hist`/`io_write_hist` in `time_print`'s DEBUG output — not summed, not disambiguated by row order alone. Not fixed this story: renaming any of the labels would violate the frozen AD-17 mandate (a renegotiation spec-4-3 already used once, for the aggregate-vs-per-call wrap question). Whoever next runs a `nsg>1` DEBUG build and needs to read `io_gather_hist`/`io_write_hist` from `time_print` should be aware multiple rows may share a label, distinguishable only by which one is the slab-ocean stream (fewer calls, since only one variable) versus the main streams (many more).
  evidence: `mod_ncout.F90:4312-4317` (`write_record_output_stream`'s own `gather_idx`/`write_idx`/`wait_idx`) vs. `mod_ncout.F90:4549-4552,4638-4641` (`writevar2d_output_stream`/`writevar3d_output_stream`'s separate `save` index vars, same label strings); `mod_service.F90:165-183,221-368` (`time_begin`/`time_print`, index-keyed not label-keyed registry). Not runtime-reproduced — no fixture used this story sets `nsg>1`; confirmed structurally via code reading only, during the 2026-08-23 bmad-review correction pass (see spec-4-3's Spec Change Log).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-instrument-diagnostic-output-gather-and-write.md`
  summary: **Real chemistry (`ichem=1`) is not currently runnable end-to-end on this NVHPC 26.3 `-Mbounds` debug build for the EURR-12 story-4-3 fixture domain — investigated in depth, not a story-4-3 blocker but worth recording for whoever next needs runtime chemistry verification here.** Tried `chemsimtype='DUST'` first: needs `<domname>_DUSTPARAM.nc` (bypassable via `ichdustparam=0` in `&chemparam`, a real code-supported fallback to uniform default erodibility) but also unconditionally needs `<domname>_AEBC.*.nc` aerosol boundary files (`iaerosol=1` is hard-wired per `chemsimtype` in `mod_che_common.F90:chem_config`, no namelist bypass), which need `RCMDATA/AERGLOB/<scenario>/aero_1.9x2.5_L26_*.nc` — confirmed completely absent (0 files in the entire `AERGLOB` tree) both locally and on the ICTP remote server checked earlier this story. **Dead end, no further action possible without fetching that dataset from its actual source.** Switched to `chemsimtype='CBMZ'` (gas-only, no `iaerosol`, sidesteps the AERGLOB problem): needed a `MZCLM`-style chemical boundary climatology, found `RCMDATA/EMAC_CLIM_CHEMBC/EMAC_avg_1999-2009_MM.nc` already has CBMZ-ready `*_VMR_inst` variable names but under the wrong path/filename convention (`PreProc/ICBC/mod_ch_icbc_clim.F90` expects `RCMDATA/OXIGLOB/mz4_avg_1999-2009_MM.nc` + a static `mz4_19990401.nc` carrying a `P0` scalar) and missing one species (`GLYALD`, absent from the source file entirely, not just unrenamed). Built `RCMDATA/OXIGLOB/` from copies with `P0=1.0` injected (verified correct via the source file's own processing history showing `P0=PS/PS`, i.e. always 1, since `hyam` is already in Pa) and a zero-filled `GLYALD_VMR_inst` placeholder added to each month. `chem_icbc` then ran cleanly (job 53633761, 592s, generated a real 10GB `EURR-12_CHBC.2009090100.nc`). Runtime then needed `ichsursrc=0` (another real namelist bypass) to skip a missing `<domname>_CHEMISS.nc` emission file. After all of the above plus the two `-Mbounds` fixes above, CBMZ got further than DUST ever did — real, populated `io_gather_hist`/`io_write_hist`/`io_wait_hist` values during the init-phase output write (job 53635891/53639790) — but then hung on wall-clock: 3+ hours fully CPU-bound (`TotalCPU=12:11:22` over `Elapsed=03:03:26` across 4 ranks, ~4.0x, ruling out an MPI deadlock) with zero additional stdout past `read_aeroppdata`'s "Updated VIS asy profile" print, almost certainly inside `colmod3` (radiation, `irrtm=0`) — CBMZ's 37 tracers make the chemistry-radiation coupling path substantially more expensive under an unoptimized `-O0` debug build. **Not investigated further (franco-directed: stop chasing runtime chemistry) — this story's final runtime evidence uses the plain `ntr=0` baseline instead** (see the `-Mbounds` entry above). Whoever next needs a working chemistry-enabled runtime config on this toolchain should either profile/optimize the `colmod3`+CBMZ interaction, try a production (non-`-Mbounds`, optimized) build instead of debug, or source the real `AERGLOB` dataset for DUST.
  evidence: DUST/AERGLOB dead end: `find /leonardo_scratch/fast/ICT26_MHPC_0/franco/RegCM-data/EURR12/RCMDATA/AERGLOB -type f | wc -l` → 0. CBMZ data prep: `RegCM-data/EURR12/RCMDATA/OXIGLOB/` (built), `chem-icbc-53633761.out` (PASS, `RESULT: PASS (chem-icbc-cbmz-story4-3)`). CHEMISS bypass: `EUR12_story4-3_serial.in`'s `&chemparam` block (since reverted alongside the rest of the CBMZ fixture changes — chemistry is off in the fixture as currently committed). Performance wall: `test-serial-53639790.out` (byte-identical tail after both the 30-min and 3-hour runs — zero forward progress), `sacct -j 53639790 --format=TotalCPU,Elapsed,MaxRSS` → `12:11:22 / 03:03:26 / 50736M`.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-instrument-diagnostic-output-gather-and-write.md`
  summary: **Likely genuine typo bug in a lateral-boundary update, surfaced by a bmad-review pass — not introduced by story 4-3, arrived via this branch's merge of `develop`/`master`.** `Main/mod_bdycod.F90:2393` (the east-boundary outflow case inside `extrapolate_surface_pressure`'s sibling boundary-update routine) uses `atm1%qx(jce2,i,k,n) = qxzeroval(n)**sfs%psa(jce2,i)` — exponentiation — while the three sibling boundary faces (`jce1`, `ice1`, `ice2`) all use multiplication (`qxzeroval(n)*sfs%psa(...)`). Per this project's own rules, boundary-condition code is "live experimental territory" to be flagged, not silently fixed. Flagging rather than touching it in this story, which owns diagnostic I/O, not dynamics/boundary code.
  evidence: `git diff 6516f0252ea5c5074cc2d152fb49b86f37cc9e00 HEAD -- Main/mod_bdycod.F90` shows this line as part of the `qxbval`→`qxzeroval` rename; the `**`/`*` asymmetry across the four boundary faces is visible in the same hunk. Independently flagged by two of the three parallel review layers (Blind Hunter and Edge-Case Hunter) run against the full branch diff during the 2026-08-23 story-4-3 review, so not a single-reviewer artifact.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-instrument-diagnostic-output-gather-and-write.md`
  summary: **Documentation/code default mismatch for the Holtslag PBL scheme, surfaced by a bmad-review pass — not introduced by story 4-3.** `Doc/README.namelist` documents `zhnew_fac = 1.0`, but `Main/mod_params.F90` sets the actual code default to `zhnew_fac = 0.33_rkx` — a real 3x difference in a live, used parameter (`Main/pbllib/mod_pbl_holtbl.F90`'s `zzhnew`/`zzhnew2` scaling, also recorded into NetCDF output metadata as `holtslag_zhnew_factor`). A user relying on the documented default would get a materially different PBL length-scale multiplier than what actually ships.
  evidence: `Doc/README.namelist` vs `Main/mod_params.F90`'s `zhnew_fac` assignment, both as of current `HEAD`. Confirmed independently by both the Blind Hunter and Verification-Gap review layers during the 2026-08-23 story-4-3 review.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-instrument-diagnostic-output-gather-and-write.md`
  summary: **PBL-height cap condition reads a frozen array index instead of the loop variable, so the cap likely never fires — surfaced by a bmad-review pass, not introduced by story 4-3.** `Main/pbllib/mod_pbl_myj.F90:521`'s new boundary-layer-height diagnosis adds `.or. z(lpbl+1) > 4000.0_rkx` to an `if` inside a loop over `k`; every other term in the same condition (and the surrounding loop logic) uses the loop's own `z(k+1)`, not the fixed `z(lpbl+1)`. As written, the 4000m cap only fires if the level *above the diagnosed PBL top itself* already exceeds 4000m — i.e., essentially never under normal atmospheric profiles — rather than capping the search as the loop descends.
  evidence: `Main/pbllib/mod_pbl_myj.F90:521` (current `HEAD`), flagged by the Edge-Case Hunter review layer during the 2026-08-23 story-4-3 review with the specific line and proposed `z(k+1)` correction.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-instrument-diagnostic-output-gather-and-write.md`
  summary: **Several further undocumented physics/tuning changes bundled into this branch via the `develop`/`master` merges, none with a changelog note or regression evidence — surfaced by a bmad-review pass, none caused by story 4-3, collected here for whoever owns that code.** (1) `Share/mod_dynparam.F90`: `mo_top_nudge` default flips `.false.`→`.true.`. (2) `Main/pbllib/mod_pbl_gfs.F90`: `ricr_ocn`/`ricr_lnd` split asymmetrically from `0.25`/`0.25` to `0.225`/`0.275`, and `zstblmax` changes `2500.0`→`3500.0`. (3) `Main/mod_moloch.F90`/`PreProc/ICBC/mod_write.F90`: `extrapolate_surface_pressure`'s lapse-rate clamp has its comparison sign flipped identically in both files (`if (lrt > govcp)` → `if (lrt < -govcp)` and the paired `else if`) — plausibly a genuine bug fix, but shipped silently in both copies. (4) `Main/mod_moloch.F90`: the `do_phy_bdy_wt` toggle (previously default `.false.`, disabling boundary weighting) is removed; the replacement unconditionally computes real per-component momentum weights via `setup_bdywt` but drops the equivalent weighting previously applied to `t`/`qv`/`qx`/`tke`/tracer tendencies in `status_update`, with no namelist switch left to disable it. (5) `Main/mod_bdycod.F90`: `setup_bdywt` is generalized to arbitrary `j1,j2,i1,i2` bounds with a changed indexing convention (`hefc(nspgx-ib+1,kzp1-k)` → `hefc(ib,k)`), called three times with three different staggered-grid bound sets, with no comment establishing equivalence to the original single-call version. (6) `Main/mod_moloch.F90:1579`: a `real(rkx), parameter :: minnum = minden` where `minden` is declared `real(rk8)` — silently narrows to single precision whenever `rkx` resolves to single precision, regardless of the double-precision source value.
  evidence: `git diff 6516f0252ea5c5074cc2d152fb49b86f37cc9e00 HEAD` for each named file/line. Items (1)-(4) and (6) from the Blind Hunter review layer, item (3)'s exact mechanism corroborated independently, item (5) from Blind Hunter; all from the 2026-08-23 story-4-3 review pass against the full branch diff.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-instrument-diagnostic-output-gather-and-write.md`
  summary: **`sfs%ustar` removed from the shared surface-flux type and its derivation duplicated verbatim into two PBL schemes instead of a shared helper — surfaced by a bmad-review pass, not caused by story 4-3.** `Main/mpplib/mod_regcm_types.F90`/`Main/mod_pbl_interface.F90` drop `sfs%ustar`; the same 4-line "ustar from momentum flux" derivation (`uflxsfx`, `vflxsfx`, `uu`, `sqrt(sqrt(uu))`) is then copy-pasted identically into both `Main/pbllib/mod_pbl_gfs.F90` and `Main/pbllib/mod_pbl_myj.F90` rather than factored into one shared function — a missed reuse opportunity from whichever change removed the shared field.
  evidence: `git diff 6516f0252ea5c5074cc2d152fb49b86f37cc9e00 HEAD` for the four named files. Flagged by the Blind Hunter review layer during the 2026-08-23 story-4-3 review.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-instrument-diagnostic-output-gather-and-write.md`
  summary: **This story's three CBMZ-chemistry crash-avoidance fixes are verified only up to "the run stops crashing," not full downstream numerical correctness — worth a second look before anyone relies on them for a real chemistry run.** `mod_cu_tiedtke.F90`'s `max(ntr,1)` padding, `mod_memutil.F90`'s `assignp1d_i` `size(a)==0` guard, and `mod_che_drydep.F90`'s `inh3>0` guard were each argued correct by construction (output-neutral padding, bounds-identical pointer remap, guard pattern matching every other call site) and confirmed to let the CBMZ run progress further than before — but that CBMZ run was itself abandoned (franco-directed, wall-clock wall in `colmod3`) before producing any output to numerically compare against a trusted baseline. The three fixes are kept because they're also needed for the plain `ntr=0` no-chemistry baseline (this story's actual evidence path), where they're lower-risk since the guarded code paths are exercised with zero-extent arrays either way.
  evidence: See the two `-Mbounds`/`inh3` entries above in this file for the fixes' own detail and reasoning. Raised as a fair evidence-tier caveat by the Blind Hunter review layer during the 2026-08-23 story-4-3 review; no new investigation performed, since franco already directed stopping the chemistry chase for this story.
